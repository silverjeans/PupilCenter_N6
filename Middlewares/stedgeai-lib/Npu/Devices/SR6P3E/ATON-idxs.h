/**
 * ----------------------------------------------------------------------
 *                           *** DO NOT EDIT ***
 *  This file was automatically generated by raSTa version 2.2.15.
 *  Copyright STMicroelectronics 2018-2025.
 * ----------------------------------------------------------------------
 */


#ifndef ATON_IDXS_H
#define ATON_IDXS_H

#ifdef __cplusplus
extern "C" {
#endif

#include <stdbool.h>
#include <stdint.h>
#include <inttypes.h>
#include <limits.h>
#include <string.h>
#include <stdio.h>


/* ************************************************************************************************************************************************************************************************** */
/*                                                                                                                                                                                                    */
/*                                                                                            ACTIV units                                                                                             */
/*                                                                                                                                                                                                    */
/* ************************************************************************************************************************************************************************************************** */

/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */
/*                                                                                                                                                                                                    */
/*                                                                                            ACTIV units                                                                                             */
/*                                                                                                                                                                                                    */
/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */

/**
 * \name Macros of the ACTIV units
 */
/*@{*/

/* **************************************************************************** ROM0 register of one of the ACTIV units ***************************************************************************** */

/** Value of idx index (expansion index) necessary for obtaining the ROM0_0 register. */
#define ATON_ACTIV_ROM0_0_IDX 0

/** Value of idx index (expansion index) necessary for obtaining the ROM0_1 register. */
#define ATON_ACTIV_ROM0_1_IDX 1

/** Value of idx index (expansion index) necessary for obtaining the ROM0_2 register. */
#define ATON_ACTIV_ROM0_2_IDX 2

/** Value of idx index (expansion index) necessary for obtaining the ROM0_3 register. */
#define ATON_ACTIV_ROM0_3_IDX 3

/** Value of idx index (expansion index) necessary for obtaining the ROM0_4 register. */
#define ATON_ACTIV_ROM0_4_IDX 4

/** Value of idx index (expansion index) necessary for obtaining the ROM0_5 register. */
#define ATON_ACTIV_ROM0_5_IDX 5

/** Value of idx index (expansion index) necessary for obtaining the ROM0_6 register. */
#define ATON_ACTIV_ROM0_6_IDX 6

/** Value of idx index (expansion index) necessary for obtaining the ROM0_7 register. */
#define ATON_ACTIV_ROM0_7_IDX 7

/** Value of idx index (expansion index) necessary for obtaining the ROM0_8 register. */
#define ATON_ACTIV_ROM0_8_IDX 8

/** Value of idx index (expansion index) necessary for obtaining the ROM0_9 register. */
#define ATON_ACTIV_ROM0_9_IDX 9

/** Value of idx index (expansion index) necessary for obtaining the ROM0_10 register. */
#define ATON_ACTIV_ROM0_10_IDX 10

/** Value of idx index (expansion index) necessary for obtaining the ROM0_11 register. */
#define ATON_ACTIV_ROM0_11_IDX 11

/** Value of idx index (expansion index) necessary for obtaining the ROM0_12 register. */
#define ATON_ACTIV_ROM0_12_IDX 12

/** Value of idx index (expansion index) necessary for obtaining the ROM0_13 register. */
#define ATON_ACTIV_ROM0_13_IDX 13

/** Value of idx index (expansion index) necessary for obtaining the ROM0_14 register. */
#define ATON_ACTIV_ROM0_14_IDX 14

/** Value of idx index (expansion index) necessary for obtaining the ROM0_15 register. */
#define ATON_ACTIV_ROM0_15_IDX 15

/** Value of idx index (expansion index) necessary for obtaining the ROM0_16 register. */
#define ATON_ACTIV_ROM0_16_IDX 16

/** Value of idx index (expansion index) necessary for obtaining the ROM0_17 register. */
#define ATON_ACTIV_ROM0_17_IDX 17

/** Value of idx index (expansion index) necessary for obtaining the ROM0_18 register. */
#define ATON_ACTIV_ROM0_18_IDX 18

/** Value of idx index (expansion index) necessary for obtaining the ROM0_19 register. */
#define ATON_ACTIV_ROM0_19_IDX 19

/** Value of idx index (expansion index) necessary for obtaining the ROM0_20 register. */
#define ATON_ACTIV_ROM0_20_IDX 20

/** Value of idx index (expansion index) necessary for obtaining the ROM0_21 register. */
#define ATON_ACTIV_ROM0_21_IDX 21

/** Value of idx index (expansion index) necessary for obtaining the ROM0_22 register. */
#define ATON_ACTIV_ROM0_22_IDX 22

/** Value of idx index (expansion index) necessary for obtaining the ROM0_23 register. */
#define ATON_ACTIV_ROM0_23_IDX 23

/** Value of idx index (expansion index) necessary for obtaining the ROM0_24 register. */
#define ATON_ACTIV_ROM0_24_IDX 24

/** Value of idx index (expansion index) necessary for obtaining the ROM0_25 register. */
#define ATON_ACTIV_ROM0_25_IDX 25

/** Value of idx index (expansion index) necessary for obtaining the ROM0_26 register. */
#define ATON_ACTIV_ROM0_26_IDX 26

/** Value of idx index (expansion index) necessary for obtaining the ROM0_27 register. */
#define ATON_ACTIV_ROM0_27_IDX 27

/** Value of idx index (expansion index) necessary for obtaining the ROM0_28 register. */
#define ATON_ACTIV_ROM0_28_IDX 28

/** Value of idx index (expansion index) necessary for obtaining the ROM0_29 register. */
#define ATON_ACTIV_ROM0_29_IDX 29

/** Value of idx index (expansion index) necessary for obtaining the ROM0_30 register. */
#define ATON_ACTIV_ROM0_30_IDX 30

/** Value of idx index (expansion index) necessary for obtaining the ROM0_31 register. */
#define ATON_ACTIV_ROM0_31_IDX 31


/* *************************************************************************** ROM1_AB register of one of the ACTIV units *************************************************************************** */

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_0 register. */
#define ATON_ACTIV_0_ROM1_AB_0_IDX 0

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_1 register. */
#define ATON_ACTIV_0_ROM1_AB_1_IDX 1

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_2 register. */
#define ATON_ACTIV_0_ROM1_AB_2_IDX 2

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_3 register. */
#define ATON_ACTIV_0_ROM1_AB_3_IDX 3

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_4 register. */
#define ATON_ACTIV_0_ROM1_AB_4_IDX 4

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_5 register. */
#define ATON_ACTIV_0_ROM1_AB_5_IDX 5

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_6 register. */
#define ATON_ACTIV_0_ROM1_AB_6_IDX 6

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_7 register. */
#define ATON_ACTIV_0_ROM1_AB_7_IDX 7

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_8 register. */
#define ATON_ACTIV_0_ROM1_AB_8_IDX 8

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_9 register. */
#define ATON_ACTIV_0_ROM1_AB_9_IDX 9

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_10 register. */
#define ATON_ACTIV_0_ROM1_AB_10_IDX 10

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_11 register. */
#define ATON_ACTIV_0_ROM1_AB_11_IDX 11

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_12 register. */
#define ATON_ACTIV_0_ROM1_AB_12_IDX 12

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_13 register. */
#define ATON_ACTIV_0_ROM1_AB_13_IDX 13

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_14 register. */
#define ATON_ACTIV_0_ROM1_AB_14_IDX 14

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_15 register. */
#define ATON_ACTIV_0_ROM1_AB_15_IDX 15

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_16 register. */
#define ATON_ACTIV_0_ROM1_AB_16_IDX 16

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_17 register. */
#define ATON_ACTIV_0_ROM1_AB_17_IDX 17

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_18 register. */
#define ATON_ACTIV_0_ROM1_AB_18_IDX 18

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_19 register. */
#define ATON_ACTIV_0_ROM1_AB_19_IDX 19

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_20 register. */
#define ATON_ACTIV_0_ROM1_AB_20_IDX 20

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_21 register. */
#define ATON_ACTIV_0_ROM1_AB_21_IDX 21

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_22 register. */
#define ATON_ACTIV_0_ROM1_AB_22_IDX 22

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_23 register. */
#define ATON_ACTIV_0_ROM1_AB_23_IDX 23

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_24 register. */
#define ATON_ACTIV_0_ROM1_AB_24_IDX 24

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_25 register. */
#define ATON_ACTIV_0_ROM1_AB_25_IDX 25

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_26 register. */
#define ATON_ACTIV_0_ROM1_AB_26_IDX 26

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_27 register. */
#define ATON_ACTIV_0_ROM1_AB_27_IDX 27

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_28 register. */
#define ATON_ACTIV_0_ROM1_AB_28_IDX 28

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_29 register. */
#define ATON_ACTIV_0_ROM1_AB_29_IDX 29

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_30 register. */
#define ATON_ACTIV_0_ROM1_AB_30_IDX 30

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_31 register. */
#define ATON_ACTIV_0_ROM1_AB_31_IDX 31

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_32 register. */
#define ATON_ACTIV_0_ROM1_AB_32_IDX 32

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_33 register. */
#define ATON_ACTIV_0_ROM1_AB_33_IDX 33

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_34 register. */
#define ATON_ACTIV_0_ROM1_AB_34_IDX 34

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_35 register. */
#define ATON_ACTIV_0_ROM1_AB_35_IDX 35

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_36 register. */
#define ATON_ACTIV_0_ROM1_AB_36_IDX 36

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_37 register. */
#define ATON_ACTIV_0_ROM1_AB_37_IDX 37

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_38 register. */
#define ATON_ACTIV_0_ROM1_AB_38_IDX 38

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_39 register. */
#define ATON_ACTIV_0_ROM1_AB_39_IDX 39

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_40 register. */
#define ATON_ACTIV_0_ROM1_AB_40_IDX 40

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_41 register. */
#define ATON_ACTIV_0_ROM1_AB_41_IDX 41

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_42 register. */
#define ATON_ACTIV_0_ROM1_AB_42_IDX 42

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_43 register. */
#define ATON_ACTIV_0_ROM1_AB_43_IDX 43

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_44 register. */
#define ATON_ACTIV_0_ROM1_AB_44_IDX 44

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_45 register. */
#define ATON_ACTIV_0_ROM1_AB_45_IDX 45

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_46 register. */
#define ATON_ACTIV_0_ROM1_AB_46_IDX 46

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_47 register. */
#define ATON_ACTIV_0_ROM1_AB_47_IDX 47

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_48 register. */
#define ATON_ACTIV_0_ROM1_AB_48_IDX 48

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_49 register. */
#define ATON_ACTIV_0_ROM1_AB_49_IDX 49

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_50 register. */
#define ATON_ACTIV_0_ROM1_AB_50_IDX 50

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_51 register. */
#define ATON_ACTIV_0_ROM1_AB_51_IDX 51

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_52 register. */
#define ATON_ACTIV_0_ROM1_AB_52_IDX 52

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_53 register. */
#define ATON_ACTIV_0_ROM1_AB_53_IDX 53

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_54 register. */
#define ATON_ACTIV_0_ROM1_AB_54_IDX 54

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_55 register. */
#define ATON_ACTIV_0_ROM1_AB_55_IDX 55

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_56 register. */
#define ATON_ACTIV_0_ROM1_AB_56_IDX 56

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_57 register. */
#define ATON_ACTIV_0_ROM1_AB_57_IDX 57

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_58 register. */
#define ATON_ACTIV_0_ROM1_AB_58_IDX 58

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_59 register. */
#define ATON_ACTIV_0_ROM1_AB_59_IDX 59

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_60 register. */
#define ATON_ACTIV_0_ROM1_AB_60_IDX 60

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_61 register. */
#define ATON_ACTIV_0_ROM1_AB_61_IDX 61

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_62 register. */
#define ATON_ACTIV_0_ROM1_AB_62_IDX 62

/** Value of idx index (expansion index) for ACTIV_0.ROM1_AB_63 register. */
#define ATON_ACTIV_0_ROM1_AB_63_IDX 63


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_0 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_0 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_1 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_1 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_10 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_10 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_10_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 10 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_11 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_11 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_11_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 11 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_12 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_12 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_12_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 12 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_13 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_13 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_13_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 13 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_14 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_14 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_14_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 14 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_15 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_15 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_15_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 15 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_16 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_16 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_16_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 16 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_17 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_17 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_17_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 17 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_18 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_18 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_18_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 18 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_19 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_19 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_19_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 19 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_2 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_2 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_20 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_20 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_20_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 20 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_21 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_21 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_21_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 21 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_22 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_22 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_22_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 22 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_23 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_23 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_23_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 23 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_24 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_24 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_24_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 24 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_25 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_25 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_25_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 25 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_26 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_26 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_26_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 26 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_27 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_27 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_27_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 27 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_28 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_28 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_28_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 28 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_29 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_29 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_29_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 29 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_3 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_3 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_3_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_30 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_30 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_30_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 30 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_31 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_31 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_31_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 31 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_32 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_32 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_32_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 32 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_33 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_33 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_33_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 33 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_34 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_34 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_34_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 34 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_35 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_35 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_35_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 35 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_36 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_36 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_36_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 36 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_37 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_37 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_37_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 37 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_38 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_38 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_38_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 38 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_39 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_39 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_39_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 39 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_4 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_4 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_4_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_40 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_40 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_40_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 40 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_41 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_41 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_41_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 41 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_42 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_42 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_42_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 42 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_43 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_43 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_43_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 43 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_44 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_44 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_44_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 44 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_45 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_45 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_45_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 45 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_46 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_46 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_46_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 46 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_47 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_47 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_47_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 47 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_48 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_48 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_48_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 48 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_49 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_49 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_49_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 49 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_5 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_5 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_5_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_50 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_50 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_50_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 50 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_51 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_51 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_51_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 51 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_52 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_52 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_52_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 52 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_53 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_53 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_53_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 53 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_54 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_54 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_54_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 54 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_55 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_55 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_55_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 55 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_56 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_56 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_56_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 56 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_57 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_57 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_57_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 57 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_58 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_58 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_58_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 58 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_59 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_59 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_59_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 59 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_6 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_6 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_6_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_60 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_60 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_60_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 60 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_61 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_61 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_61_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 61 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_62 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_62 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_62_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 62 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_63 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_63 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_63_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 63 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_7 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_7 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_7_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 7 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_8 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_8 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_8_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 8 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_9 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_AB_9 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_AB_ROM1_AB_9_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 9 : -1)



/* *************************************************************************** ROM1_C register of one of the ACTIV units **************************************************************************** */

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_0 register. */
#define ATON_ACTIV_0_ROM1_C_0_IDX 0

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_1 register. */
#define ATON_ACTIV_0_ROM1_C_1_IDX 1

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_2 register. */
#define ATON_ACTIV_0_ROM1_C_2_IDX 2

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_3 register. */
#define ATON_ACTIV_0_ROM1_C_3_IDX 3

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_4 register. */
#define ATON_ACTIV_0_ROM1_C_4_IDX 4

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_5 register. */
#define ATON_ACTIV_0_ROM1_C_5_IDX 5

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_6 register. */
#define ATON_ACTIV_0_ROM1_C_6_IDX 6

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_7 register. */
#define ATON_ACTIV_0_ROM1_C_7_IDX 7

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_8 register. */
#define ATON_ACTIV_0_ROM1_C_8_IDX 8

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_9 register. */
#define ATON_ACTIV_0_ROM1_C_9_IDX 9

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_10 register. */
#define ATON_ACTIV_0_ROM1_C_10_IDX 10

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_11 register. */
#define ATON_ACTIV_0_ROM1_C_11_IDX 11

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_12 register. */
#define ATON_ACTIV_0_ROM1_C_12_IDX 12

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_13 register. */
#define ATON_ACTIV_0_ROM1_C_13_IDX 13

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_14 register. */
#define ATON_ACTIV_0_ROM1_C_14_IDX 14

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_15 register. */
#define ATON_ACTIV_0_ROM1_C_15_IDX 15

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_16 register. */
#define ATON_ACTIV_0_ROM1_C_16_IDX 16

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_17 register. */
#define ATON_ACTIV_0_ROM1_C_17_IDX 17

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_18 register. */
#define ATON_ACTIV_0_ROM1_C_18_IDX 18

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_19 register. */
#define ATON_ACTIV_0_ROM1_C_19_IDX 19

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_20 register. */
#define ATON_ACTIV_0_ROM1_C_20_IDX 20

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_21 register. */
#define ATON_ACTIV_0_ROM1_C_21_IDX 21

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_22 register. */
#define ATON_ACTIV_0_ROM1_C_22_IDX 22

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_23 register. */
#define ATON_ACTIV_0_ROM1_C_23_IDX 23

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_24 register. */
#define ATON_ACTIV_0_ROM1_C_24_IDX 24

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_25 register. */
#define ATON_ACTIV_0_ROM1_C_25_IDX 25

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_26 register. */
#define ATON_ACTIV_0_ROM1_C_26_IDX 26

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_27 register. */
#define ATON_ACTIV_0_ROM1_C_27_IDX 27

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_28 register. */
#define ATON_ACTIV_0_ROM1_C_28_IDX 28

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_29 register. */
#define ATON_ACTIV_0_ROM1_C_29_IDX 29

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_30 register. */
#define ATON_ACTIV_0_ROM1_C_30_IDX 30

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_31 register. */
#define ATON_ACTIV_0_ROM1_C_31_IDX 31

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_32 register. */
#define ATON_ACTIV_0_ROM1_C_32_IDX 32

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_33 register. */
#define ATON_ACTIV_0_ROM1_C_33_IDX 33

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_34 register. */
#define ATON_ACTIV_0_ROM1_C_34_IDX 34

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_35 register. */
#define ATON_ACTIV_0_ROM1_C_35_IDX 35

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_36 register. */
#define ATON_ACTIV_0_ROM1_C_36_IDX 36

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_37 register. */
#define ATON_ACTIV_0_ROM1_C_37_IDX 37

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_38 register. */
#define ATON_ACTIV_0_ROM1_C_38_IDX 38

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_39 register. */
#define ATON_ACTIV_0_ROM1_C_39_IDX 39

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_40 register. */
#define ATON_ACTIV_0_ROM1_C_40_IDX 40

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_41 register. */
#define ATON_ACTIV_0_ROM1_C_41_IDX 41

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_42 register. */
#define ATON_ACTIV_0_ROM1_C_42_IDX 42

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_43 register. */
#define ATON_ACTIV_0_ROM1_C_43_IDX 43

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_44 register. */
#define ATON_ACTIV_0_ROM1_C_44_IDX 44

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_45 register. */
#define ATON_ACTIV_0_ROM1_C_45_IDX 45

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_46 register. */
#define ATON_ACTIV_0_ROM1_C_46_IDX 46

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_47 register. */
#define ATON_ACTIV_0_ROM1_C_47_IDX 47

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_48 register. */
#define ATON_ACTIV_0_ROM1_C_48_IDX 48

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_49 register. */
#define ATON_ACTIV_0_ROM1_C_49_IDX 49

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_50 register. */
#define ATON_ACTIV_0_ROM1_C_50_IDX 50

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_51 register. */
#define ATON_ACTIV_0_ROM1_C_51_IDX 51

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_52 register. */
#define ATON_ACTIV_0_ROM1_C_52_IDX 52

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_53 register. */
#define ATON_ACTIV_0_ROM1_C_53_IDX 53

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_54 register. */
#define ATON_ACTIV_0_ROM1_C_54_IDX 54

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_55 register. */
#define ATON_ACTIV_0_ROM1_C_55_IDX 55

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_56 register. */
#define ATON_ACTIV_0_ROM1_C_56_IDX 56

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_57 register. */
#define ATON_ACTIV_0_ROM1_C_57_IDX 57

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_58 register. */
#define ATON_ACTIV_0_ROM1_C_58_IDX 58

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_59 register. */
#define ATON_ACTIV_0_ROM1_C_59_IDX 59

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_60 register. */
#define ATON_ACTIV_0_ROM1_C_60_IDX 60

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_61 register. */
#define ATON_ACTIV_0_ROM1_C_61_IDX 61

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_62 register. */
#define ATON_ACTIV_0_ROM1_C_62_IDX 62

/** Value of idx index (expansion index) for ACTIV_0.ROM1_C_63 register. */
#define ATON_ACTIV_0_ROM1_C_63_IDX 63


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_0 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_0 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_1 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_1 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_10 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_10 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_10_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 10 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_11 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_11 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_11_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 11 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_12 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_12 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_12_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 12 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_13 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_13 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_13_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 13 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_14 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_14 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_14_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 14 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_15 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_15 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_15_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 15 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_16 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_16 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_16_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 16 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_17 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_17 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_17_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 17 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_18 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_18 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_18_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 18 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_19 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_19 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_19_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 19 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_2 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_2 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_20 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_20 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_20_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 20 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_21 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_21 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_21_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 21 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_22 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_22 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_22_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 22 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_23 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_23 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_23_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 23 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_24 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_24 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_24_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 24 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_25 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_25 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_25_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 25 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_26 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_26 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_26_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 26 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_27 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_27 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_27_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 27 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_28 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_28 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_28_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 28 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_29 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_29 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_29_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 29 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_3 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_3 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_3_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_30 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_30 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_30_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 30 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_31 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_31 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_31_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 31 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_32 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_32 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_32_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 32 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_33 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_33 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_33_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 33 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_34 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_34 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_34_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 34 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_35 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_35 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_35_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 35 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_36 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_36 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_36_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 36 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_37 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_37 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_37_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 37 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_38 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_38 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_38_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 38 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_39 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_39 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_39_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 39 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_4 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_4 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_4_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_40 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_40 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_40_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 40 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_41 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_41 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_41_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 41 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_42 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_42 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_42_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 42 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_43 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_43 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_43_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 43 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_44 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_44 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_44_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 44 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_45 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_45 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_45_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 45 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_46 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_46 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_46_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 46 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_47 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_47 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_47_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 47 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_48 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_48 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_48_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 48 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_49 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_49 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_49_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 49 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_5 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_5 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_5_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_50 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_50 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_50_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 50 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_51 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_51 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_51_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 51 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_52 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_52 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_52_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 52 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_53 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_53 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_53_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 53 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_54 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_54 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_54_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 54 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_55 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_55 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_55_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 55 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_56 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_56 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_56_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 56 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_57 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_57 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_57_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 57 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_58 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_58 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_58_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 58 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_59 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_59 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_59_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 59 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_6 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_6 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_6_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_60 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_60 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_60_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 60 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_61 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_61 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_61_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 61 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_62 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_62 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_62_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 62 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_63 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_63 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_63_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 63 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_7 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_7 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_7_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 7 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_8 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_8 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_8_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 8 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ROM1_C_9 register given the index of one of the ACTIV units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'ACTIV_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ROM1_C_9 register given the index \e UNIT of one of the ACTIV units
 */

#define ATON_ACTIV_ROM1_C_ROM1_C_9_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 9 : -1)



/* ***************************************************************************** LUT register of one of the ACTIV units ***************************************************************************** */

/** Value of idx index (expansion index) necessary for obtaining the LUT_0 register. */
#define ATON_ACTIV_LUT_0_IDX 0

/** Value of idx index (expansion index) necessary for obtaining the LUT_1 register. */
#define ATON_ACTIV_LUT_1_IDX 1

/** Value of idx index (expansion index) necessary for obtaining the LUT_2 register. */
#define ATON_ACTIV_LUT_2_IDX 2

/** Value of idx index (expansion index) necessary for obtaining the LUT_3 register. */
#define ATON_ACTIV_LUT_3_IDX 3

/** Value of idx index (expansion index) necessary for obtaining the LUT_4 register. */
#define ATON_ACTIV_LUT_4_IDX 4

/** Value of idx index (expansion index) necessary for obtaining the LUT_5 register. */
#define ATON_ACTIV_LUT_5_IDX 5

/** Value of idx index (expansion index) necessary for obtaining the LUT_6 register. */
#define ATON_ACTIV_LUT_6_IDX 6

/** Value of idx index (expansion index) necessary for obtaining the LUT_7 register. */
#define ATON_ACTIV_LUT_7_IDX 7

/** Value of idx index (expansion index) necessary for obtaining the LUT_8 register. */
#define ATON_ACTIV_LUT_8_IDX 8

/** Value of idx index (expansion index) necessary for obtaining the LUT_9 register. */
#define ATON_ACTIV_LUT_9_IDX 9

/** Value of idx index (expansion index) necessary for obtaining the LUT_10 register. */
#define ATON_ACTIV_LUT_10_IDX 10

/** Value of idx index (expansion index) necessary for obtaining the LUT_11 register. */
#define ATON_ACTIV_LUT_11_IDX 11

/** Value of idx index (expansion index) necessary for obtaining the LUT_12 register. */
#define ATON_ACTIV_LUT_12_IDX 12

/** Value of idx index (expansion index) necessary for obtaining the LUT_13 register. */
#define ATON_ACTIV_LUT_13_IDX 13

/** Value of idx index (expansion index) necessary for obtaining the LUT_14 register. */
#define ATON_ACTIV_LUT_14_IDX 14

/** Value of idx index (expansion index) necessary for obtaining the LUT_15 register. */
#define ATON_ACTIV_LUT_15_IDX 15

/** Value of idx index (expansion index) necessary for obtaining the LUT_16 register. */
#define ATON_ACTIV_LUT_16_IDX 16

/** Value of idx index (expansion index) necessary for obtaining the LUT_17 register. */
#define ATON_ACTIV_LUT_17_IDX 17

/** Value of idx index (expansion index) necessary for obtaining the LUT_18 register. */
#define ATON_ACTIV_LUT_18_IDX 18

/** Value of idx index (expansion index) necessary for obtaining the LUT_19 register. */
#define ATON_ACTIV_LUT_19_IDX 19

/** Value of idx index (expansion index) necessary for obtaining the LUT_20 register. */
#define ATON_ACTIV_LUT_20_IDX 20

/** Value of idx index (expansion index) necessary for obtaining the LUT_21 register. */
#define ATON_ACTIV_LUT_21_IDX 21

/** Value of idx index (expansion index) necessary for obtaining the LUT_22 register. */
#define ATON_ACTIV_LUT_22_IDX 22

/** Value of idx index (expansion index) necessary for obtaining the LUT_23 register. */
#define ATON_ACTIV_LUT_23_IDX 23

/** Value of idx index (expansion index) necessary for obtaining the LUT_24 register. */
#define ATON_ACTIV_LUT_24_IDX 24

/** Value of idx index (expansion index) necessary for obtaining the LUT_25 register. */
#define ATON_ACTIV_LUT_25_IDX 25

/** Value of idx index (expansion index) necessary for obtaining the LUT_26 register. */
#define ATON_ACTIV_LUT_26_IDX 26

/** Value of idx index (expansion index) necessary for obtaining the LUT_27 register. */
#define ATON_ACTIV_LUT_27_IDX 27

/** Value of idx index (expansion index) necessary for obtaining the LUT_28 register. */
#define ATON_ACTIV_LUT_28_IDX 28

/** Value of idx index (expansion index) necessary for obtaining the LUT_29 register. */
#define ATON_ACTIV_LUT_29_IDX 29

/** Value of idx index (expansion index) necessary for obtaining the LUT_30 register. */
#define ATON_ACTIV_LUT_30_IDX 30

/** Value of idx index (expansion index) necessary for obtaining the LUT_31 register. */
#define ATON_ACTIV_LUT_31_IDX 31

/** Value of idx index (expansion index) necessary for obtaining the LUT_32 register. */
#define ATON_ACTIV_LUT_32_IDX 32

/** Value of idx index (expansion index) necessary for obtaining the LUT_33 register. */
#define ATON_ACTIV_LUT_33_IDX 33

/** Value of idx index (expansion index) necessary for obtaining the LUT_34 register. */
#define ATON_ACTIV_LUT_34_IDX 34

/** Value of idx index (expansion index) necessary for obtaining the LUT_35 register. */
#define ATON_ACTIV_LUT_35_IDX 35

/** Value of idx index (expansion index) necessary for obtaining the LUT_36 register. */
#define ATON_ACTIV_LUT_36_IDX 36

/** Value of idx index (expansion index) necessary for obtaining the LUT_37 register. */
#define ATON_ACTIV_LUT_37_IDX 37

/** Value of idx index (expansion index) necessary for obtaining the LUT_38 register. */
#define ATON_ACTIV_LUT_38_IDX 38

/** Value of idx index (expansion index) necessary for obtaining the LUT_39 register. */
#define ATON_ACTIV_LUT_39_IDX 39

/** Value of idx index (expansion index) necessary for obtaining the LUT_40 register. */
#define ATON_ACTIV_LUT_40_IDX 40

/** Value of idx index (expansion index) necessary for obtaining the LUT_41 register. */
#define ATON_ACTIV_LUT_41_IDX 41

/** Value of idx index (expansion index) necessary for obtaining the LUT_42 register. */
#define ATON_ACTIV_LUT_42_IDX 42

/** Value of idx index (expansion index) necessary for obtaining the LUT_43 register. */
#define ATON_ACTIV_LUT_43_IDX 43

/** Value of idx index (expansion index) necessary for obtaining the LUT_44 register. */
#define ATON_ACTIV_LUT_44_IDX 44

/** Value of idx index (expansion index) necessary for obtaining the LUT_45 register. */
#define ATON_ACTIV_LUT_45_IDX 45

/** Value of idx index (expansion index) necessary for obtaining the LUT_46 register. */
#define ATON_ACTIV_LUT_46_IDX 46

/** Value of idx index (expansion index) necessary for obtaining the LUT_47 register. */
#define ATON_ACTIV_LUT_47_IDX 47

/** Value of idx index (expansion index) necessary for obtaining the LUT_48 register. */
#define ATON_ACTIV_LUT_48_IDX 48

/** Value of idx index (expansion index) necessary for obtaining the LUT_49 register. */
#define ATON_ACTIV_LUT_49_IDX 49

/** Value of idx index (expansion index) necessary for obtaining the LUT_50 register. */
#define ATON_ACTIV_LUT_50_IDX 50

/** Value of idx index (expansion index) necessary for obtaining the LUT_51 register. */
#define ATON_ACTIV_LUT_51_IDX 51

/** Value of idx index (expansion index) necessary for obtaining the LUT_52 register. */
#define ATON_ACTIV_LUT_52_IDX 52

/** Value of idx index (expansion index) necessary for obtaining the LUT_53 register. */
#define ATON_ACTIV_LUT_53_IDX 53

/** Value of idx index (expansion index) necessary for obtaining the LUT_54 register. */
#define ATON_ACTIV_LUT_54_IDX 54

/** Value of idx index (expansion index) necessary for obtaining the LUT_55 register. */
#define ATON_ACTIV_LUT_55_IDX 55

/** Value of idx index (expansion index) necessary for obtaining the LUT_56 register. */
#define ATON_ACTIV_LUT_56_IDX 56

/** Value of idx index (expansion index) necessary for obtaining the LUT_57 register. */
#define ATON_ACTIV_LUT_57_IDX 57

/** Value of idx index (expansion index) necessary for obtaining the LUT_58 register. */
#define ATON_ACTIV_LUT_58_IDX 58

/** Value of idx index (expansion index) necessary for obtaining the LUT_59 register. */
#define ATON_ACTIV_LUT_59_IDX 59

/** Value of idx index (expansion index) necessary for obtaining the LUT_60 register. */
#define ATON_ACTIV_LUT_60_IDX 60

/** Value of idx index (expansion index) necessary for obtaining the LUT_61 register. */
#define ATON_ACTIV_LUT_61_IDX 61

/** Value of idx index (expansion index) necessary for obtaining the LUT_62 register. */
#define ATON_ACTIV_LUT_62_IDX 62

/** Value of idx index (expansion index) necessary for obtaining the LUT_63 register. */
#define ATON_ACTIV_LUT_63_IDX 63


/*@}*/


/* ************************************************************************************************************************************************************************************************** */
/*                                                                                                                                                                                                    */
/*                                                                                            ARITH units                                                                                             */
/*                                                                                                                                                                                                    */
/* ************************************************************************************************************************************************************************************************** */

/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */
/*                                                                                                                                                                                                    */
/*                                                                                            ARITH units                                                                                             */
/*                                                                                                                                                                                                    */
/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */

/**
 * \name Macros of the ARITH units
 */
/*@{*/

/* ************************************************************************** COEFF_AB register of one of the ARITH units *************************************************************************** */

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_0 register. */
#define ATON_ARITH_COEFF_AB_0_IDX 0

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_1 register. */
#define ATON_ARITH_COEFF_AB_1_IDX 1

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_2 register. */
#define ATON_ARITH_COEFF_AB_2_IDX 2

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_3 register. */
#define ATON_ARITH_COEFF_AB_3_IDX 3

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_4 register. */
#define ATON_ARITH_COEFF_AB_4_IDX 4

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_5 register. */
#define ATON_ARITH_COEFF_AB_5_IDX 5

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_6 register. */
#define ATON_ARITH_COEFF_AB_6_IDX 6

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_7 register. */
#define ATON_ARITH_COEFF_AB_7_IDX 7

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_8 register. */
#define ATON_ARITH_COEFF_AB_8_IDX 8

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_9 register. */
#define ATON_ARITH_COEFF_AB_9_IDX 9

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_10 register. */
#define ATON_ARITH_COEFF_AB_10_IDX 10

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_11 register. */
#define ATON_ARITH_COEFF_AB_11_IDX 11

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_12 register. */
#define ATON_ARITH_COEFF_AB_12_IDX 12

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_13 register. */
#define ATON_ARITH_COEFF_AB_13_IDX 13

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_14 register. */
#define ATON_ARITH_COEFF_AB_14_IDX 14

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_15 register. */
#define ATON_ARITH_COEFF_AB_15_IDX 15

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_16 register. */
#define ATON_ARITH_COEFF_AB_16_IDX 16

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_17 register. */
#define ATON_ARITH_COEFF_AB_17_IDX 17

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_18 register. */
#define ATON_ARITH_COEFF_AB_18_IDX 18

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_19 register. */
#define ATON_ARITH_COEFF_AB_19_IDX 19

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_20 register. */
#define ATON_ARITH_COEFF_AB_20_IDX 20

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_21 register. */
#define ATON_ARITH_COEFF_AB_21_IDX 21

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_22 register. */
#define ATON_ARITH_COEFF_AB_22_IDX 22

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_23 register. */
#define ATON_ARITH_COEFF_AB_23_IDX 23

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_24 register. */
#define ATON_ARITH_COEFF_AB_24_IDX 24

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_25 register. */
#define ATON_ARITH_COEFF_AB_25_IDX 25

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_26 register. */
#define ATON_ARITH_COEFF_AB_26_IDX 26

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_27 register. */
#define ATON_ARITH_COEFF_AB_27_IDX 27

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_28 register. */
#define ATON_ARITH_COEFF_AB_28_IDX 28

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_29 register. */
#define ATON_ARITH_COEFF_AB_29_IDX 29

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_30 register. */
#define ATON_ARITH_COEFF_AB_30_IDX 30

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_31 register. */
#define ATON_ARITH_COEFF_AB_31_IDX 31

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_32 register. */
#define ATON_ARITH_COEFF_AB_32_IDX 32

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_33 register. */
#define ATON_ARITH_COEFF_AB_33_IDX 33

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_34 register. */
#define ATON_ARITH_COEFF_AB_34_IDX 34

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_35 register. */
#define ATON_ARITH_COEFF_AB_35_IDX 35

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_36 register. */
#define ATON_ARITH_COEFF_AB_36_IDX 36

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_37 register. */
#define ATON_ARITH_COEFF_AB_37_IDX 37

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_38 register. */
#define ATON_ARITH_COEFF_AB_38_IDX 38

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_39 register. */
#define ATON_ARITH_COEFF_AB_39_IDX 39

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_40 register. */
#define ATON_ARITH_COEFF_AB_40_IDX 40

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_41 register. */
#define ATON_ARITH_COEFF_AB_41_IDX 41

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_42 register. */
#define ATON_ARITH_COEFF_AB_42_IDX 42

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_43 register. */
#define ATON_ARITH_COEFF_AB_43_IDX 43

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_44 register. */
#define ATON_ARITH_COEFF_AB_44_IDX 44

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_45 register. */
#define ATON_ARITH_COEFF_AB_45_IDX 45

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_46 register. */
#define ATON_ARITH_COEFF_AB_46_IDX 46

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_47 register. */
#define ATON_ARITH_COEFF_AB_47_IDX 47

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_48 register. */
#define ATON_ARITH_COEFF_AB_48_IDX 48

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_49 register. */
#define ATON_ARITH_COEFF_AB_49_IDX 49

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_50 register. */
#define ATON_ARITH_COEFF_AB_50_IDX 50

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_51 register. */
#define ATON_ARITH_COEFF_AB_51_IDX 51

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_52 register. */
#define ATON_ARITH_COEFF_AB_52_IDX 52

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_53 register. */
#define ATON_ARITH_COEFF_AB_53_IDX 53

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_54 register. */
#define ATON_ARITH_COEFF_AB_54_IDX 54

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_55 register. */
#define ATON_ARITH_COEFF_AB_55_IDX 55

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_56 register. */
#define ATON_ARITH_COEFF_AB_56_IDX 56

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_57 register. */
#define ATON_ARITH_COEFF_AB_57_IDX 57

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_58 register. */
#define ATON_ARITH_COEFF_AB_58_IDX 58

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_59 register. */
#define ATON_ARITH_COEFF_AB_59_IDX 59

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_60 register. */
#define ATON_ARITH_COEFF_AB_60_IDX 60

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_61 register. */
#define ATON_ARITH_COEFF_AB_61_IDX 61

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_62 register. */
#define ATON_ARITH_COEFF_AB_62_IDX 62

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_63 register. */
#define ATON_ARITH_COEFF_AB_63_IDX 63

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_64 register. */
#define ATON_ARITH_COEFF_AB_64_IDX 64

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_65 register. */
#define ATON_ARITH_COEFF_AB_65_IDX 65

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_66 register. */
#define ATON_ARITH_COEFF_AB_66_IDX 66

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_67 register. */
#define ATON_ARITH_COEFF_AB_67_IDX 67

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_68 register. */
#define ATON_ARITH_COEFF_AB_68_IDX 68

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_69 register. */
#define ATON_ARITH_COEFF_AB_69_IDX 69

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_70 register. */
#define ATON_ARITH_COEFF_AB_70_IDX 70

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_71 register. */
#define ATON_ARITH_COEFF_AB_71_IDX 71

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_72 register. */
#define ATON_ARITH_COEFF_AB_72_IDX 72

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_73 register. */
#define ATON_ARITH_COEFF_AB_73_IDX 73

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_74 register. */
#define ATON_ARITH_COEFF_AB_74_IDX 74

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_75 register. */
#define ATON_ARITH_COEFF_AB_75_IDX 75

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_76 register. */
#define ATON_ARITH_COEFF_AB_76_IDX 76

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_77 register. */
#define ATON_ARITH_COEFF_AB_77_IDX 77

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_78 register. */
#define ATON_ARITH_COEFF_AB_78_IDX 78

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_79 register. */
#define ATON_ARITH_COEFF_AB_79_IDX 79

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_80 register. */
#define ATON_ARITH_COEFF_AB_80_IDX 80

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_81 register. */
#define ATON_ARITH_COEFF_AB_81_IDX 81

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_82 register. */
#define ATON_ARITH_COEFF_AB_82_IDX 82

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_83 register. */
#define ATON_ARITH_COEFF_AB_83_IDX 83

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_84 register. */
#define ATON_ARITH_COEFF_AB_84_IDX 84

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_85 register. */
#define ATON_ARITH_COEFF_AB_85_IDX 85

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_86 register. */
#define ATON_ARITH_COEFF_AB_86_IDX 86

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_87 register. */
#define ATON_ARITH_COEFF_AB_87_IDX 87

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_88 register. */
#define ATON_ARITH_COEFF_AB_88_IDX 88

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_89 register. */
#define ATON_ARITH_COEFF_AB_89_IDX 89

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_90 register. */
#define ATON_ARITH_COEFF_AB_90_IDX 90

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_91 register. */
#define ATON_ARITH_COEFF_AB_91_IDX 91

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_92 register. */
#define ATON_ARITH_COEFF_AB_92_IDX 92

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_93 register. */
#define ATON_ARITH_COEFF_AB_93_IDX 93

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_94 register. */
#define ATON_ARITH_COEFF_AB_94_IDX 94

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_95 register. */
#define ATON_ARITH_COEFF_AB_95_IDX 95

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_96 register. */
#define ATON_ARITH_COEFF_AB_96_IDX 96

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_97 register. */
#define ATON_ARITH_COEFF_AB_97_IDX 97

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_98 register. */
#define ATON_ARITH_COEFF_AB_98_IDX 98

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_99 register. */
#define ATON_ARITH_COEFF_AB_99_IDX 99

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_100 register. */
#define ATON_ARITH_COEFF_AB_100_IDX 100

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_101 register. */
#define ATON_ARITH_COEFF_AB_101_IDX 101

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_102 register. */
#define ATON_ARITH_COEFF_AB_102_IDX 102

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_103 register. */
#define ATON_ARITH_COEFF_AB_103_IDX 103

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_104 register. */
#define ATON_ARITH_COEFF_AB_104_IDX 104

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_105 register. */
#define ATON_ARITH_COEFF_AB_105_IDX 105

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_106 register. */
#define ATON_ARITH_COEFF_AB_106_IDX 106

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_107 register. */
#define ATON_ARITH_COEFF_AB_107_IDX 107

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_108 register. */
#define ATON_ARITH_COEFF_AB_108_IDX 108

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_109 register. */
#define ATON_ARITH_COEFF_AB_109_IDX 109

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_110 register. */
#define ATON_ARITH_COEFF_AB_110_IDX 110

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_111 register. */
#define ATON_ARITH_COEFF_AB_111_IDX 111

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_112 register. */
#define ATON_ARITH_COEFF_AB_112_IDX 112

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_113 register. */
#define ATON_ARITH_COEFF_AB_113_IDX 113

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_114 register. */
#define ATON_ARITH_COEFF_AB_114_IDX 114

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_115 register. */
#define ATON_ARITH_COEFF_AB_115_IDX 115

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_116 register. */
#define ATON_ARITH_COEFF_AB_116_IDX 116

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_117 register. */
#define ATON_ARITH_COEFF_AB_117_IDX 117

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_118 register. */
#define ATON_ARITH_COEFF_AB_118_IDX 118

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_119 register. */
#define ATON_ARITH_COEFF_AB_119_IDX 119

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_120 register. */
#define ATON_ARITH_COEFF_AB_120_IDX 120

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_121 register. */
#define ATON_ARITH_COEFF_AB_121_IDX 121

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_122 register. */
#define ATON_ARITH_COEFF_AB_122_IDX 122

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_123 register. */
#define ATON_ARITH_COEFF_AB_123_IDX 123

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_124 register. */
#define ATON_ARITH_COEFF_AB_124_IDX 124

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_125 register. */
#define ATON_ARITH_COEFF_AB_125_IDX 125

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_126 register. */
#define ATON_ARITH_COEFF_AB_126_IDX 126

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_127 register. */
#define ATON_ARITH_COEFF_AB_127_IDX 127

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_128 register. */
#define ATON_ARITH_COEFF_AB_128_IDX 128

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_129 register. */
#define ATON_ARITH_COEFF_AB_129_IDX 129

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_130 register. */
#define ATON_ARITH_COEFF_AB_130_IDX 130

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_131 register. */
#define ATON_ARITH_COEFF_AB_131_IDX 131

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_132 register. */
#define ATON_ARITH_COEFF_AB_132_IDX 132

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_133 register. */
#define ATON_ARITH_COEFF_AB_133_IDX 133

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_134 register. */
#define ATON_ARITH_COEFF_AB_134_IDX 134

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_135 register. */
#define ATON_ARITH_COEFF_AB_135_IDX 135

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_136 register. */
#define ATON_ARITH_COEFF_AB_136_IDX 136

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_137 register. */
#define ATON_ARITH_COEFF_AB_137_IDX 137

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_138 register. */
#define ATON_ARITH_COEFF_AB_138_IDX 138

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_139 register. */
#define ATON_ARITH_COEFF_AB_139_IDX 139

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_140 register. */
#define ATON_ARITH_COEFF_AB_140_IDX 140

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_141 register. */
#define ATON_ARITH_COEFF_AB_141_IDX 141

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_142 register. */
#define ATON_ARITH_COEFF_AB_142_IDX 142

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_143 register. */
#define ATON_ARITH_COEFF_AB_143_IDX 143

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_144 register. */
#define ATON_ARITH_COEFF_AB_144_IDX 144

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_145 register. */
#define ATON_ARITH_COEFF_AB_145_IDX 145

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_146 register. */
#define ATON_ARITH_COEFF_AB_146_IDX 146

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_147 register. */
#define ATON_ARITH_COEFF_AB_147_IDX 147

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_148 register. */
#define ATON_ARITH_COEFF_AB_148_IDX 148

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_149 register. */
#define ATON_ARITH_COEFF_AB_149_IDX 149

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_150 register. */
#define ATON_ARITH_COEFF_AB_150_IDX 150

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_151 register. */
#define ATON_ARITH_COEFF_AB_151_IDX 151

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_152 register. */
#define ATON_ARITH_COEFF_AB_152_IDX 152

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_153 register. */
#define ATON_ARITH_COEFF_AB_153_IDX 153

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_154 register. */
#define ATON_ARITH_COEFF_AB_154_IDX 154

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_155 register. */
#define ATON_ARITH_COEFF_AB_155_IDX 155

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_156 register. */
#define ATON_ARITH_COEFF_AB_156_IDX 156

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_157 register. */
#define ATON_ARITH_COEFF_AB_157_IDX 157

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_158 register. */
#define ATON_ARITH_COEFF_AB_158_IDX 158

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_159 register. */
#define ATON_ARITH_COEFF_AB_159_IDX 159

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_160 register. */
#define ATON_ARITH_COEFF_AB_160_IDX 160

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_161 register. */
#define ATON_ARITH_COEFF_AB_161_IDX 161

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_162 register. */
#define ATON_ARITH_COEFF_AB_162_IDX 162

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_163 register. */
#define ATON_ARITH_COEFF_AB_163_IDX 163

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_164 register. */
#define ATON_ARITH_COEFF_AB_164_IDX 164

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_165 register. */
#define ATON_ARITH_COEFF_AB_165_IDX 165

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_166 register. */
#define ATON_ARITH_COEFF_AB_166_IDX 166

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_167 register. */
#define ATON_ARITH_COEFF_AB_167_IDX 167

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_168 register. */
#define ATON_ARITH_COEFF_AB_168_IDX 168

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_169 register. */
#define ATON_ARITH_COEFF_AB_169_IDX 169

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_170 register. */
#define ATON_ARITH_COEFF_AB_170_IDX 170

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_171 register. */
#define ATON_ARITH_COEFF_AB_171_IDX 171

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_172 register. */
#define ATON_ARITH_COEFF_AB_172_IDX 172

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_173 register. */
#define ATON_ARITH_COEFF_AB_173_IDX 173

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_174 register. */
#define ATON_ARITH_COEFF_AB_174_IDX 174

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_175 register. */
#define ATON_ARITH_COEFF_AB_175_IDX 175

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_176 register. */
#define ATON_ARITH_COEFF_AB_176_IDX 176

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_177 register. */
#define ATON_ARITH_COEFF_AB_177_IDX 177

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_178 register. */
#define ATON_ARITH_COEFF_AB_178_IDX 178

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_179 register. */
#define ATON_ARITH_COEFF_AB_179_IDX 179

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_180 register. */
#define ATON_ARITH_COEFF_AB_180_IDX 180

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_181 register. */
#define ATON_ARITH_COEFF_AB_181_IDX 181

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_182 register. */
#define ATON_ARITH_COEFF_AB_182_IDX 182

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_183 register. */
#define ATON_ARITH_COEFF_AB_183_IDX 183

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_184 register. */
#define ATON_ARITH_COEFF_AB_184_IDX 184

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_185 register. */
#define ATON_ARITH_COEFF_AB_185_IDX 185

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_186 register. */
#define ATON_ARITH_COEFF_AB_186_IDX 186

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_187 register. */
#define ATON_ARITH_COEFF_AB_187_IDX 187

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_188 register. */
#define ATON_ARITH_COEFF_AB_188_IDX 188

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_189 register. */
#define ATON_ARITH_COEFF_AB_189_IDX 189

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_190 register. */
#define ATON_ARITH_COEFF_AB_190_IDX 190

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_191 register. */
#define ATON_ARITH_COEFF_AB_191_IDX 191

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_192 register. */
#define ATON_ARITH_COEFF_AB_192_IDX 192

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_193 register. */
#define ATON_ARITH_COEFF_AB_193_IDX 193

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_194 register. */
#define ATON_ARITH_COEFF_AB_194_IDX 194

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_195 register. */
#define ATON_ARITH_COEFF_AB_195_IDX 195

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_196 register. */
#define ATON_ARITH_COEFF_AB_196_IDX 196

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_197 register. */
#define ATON_ARITH_COEFF_AB_197_IDX 197

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_198 register. */
#define ATON_ARITH_COEFF_AB_198_IDX 198

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_199 register. */
#define ATON_ARITH_COEFF_AB_199_IDX 199

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_200 register. */
#define ATON_ARITH_COEFF_AB_200_IDX 200

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_201 register. */
#define ATON_ARITH_COEFF_AB_201_IDX 201

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_202 register. */
#define ATON_ARITH_COEFF_AB_202_IDX 202

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_203 register. */
#define ATON_ARITH_COEFF_AB_203_IDX 203

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_204 register. */
#define ATON_ARITH_COEFF_AB_204_IDX 204

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_205 register. */
#define ATON_ARITH_COEFF_AB_205_IDX 205

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_206 register. */
#define ATON_ARITH_COEFF_AB_206_IDX 206

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_207 register. */
#define ATON_ARITH_COEFF_AB_207_IDX 207

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_208 register. */
#define ATON_ARITH_COEFF_AB_208_IDX 208

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_209 register. */
#define ATON_ARITH_COEFF_AB_209_IDX 209

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_210 register. */
#define ATON_ARITH_COEFF_AB_210_IDX 210

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_211 register. */
#define ATON_ARITH_COEFF_AB_211_IDX 211

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_212 register. */
#define ATON_ARITH_COEFF_AB_212_IDX 212

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_213 register. */
#define ATON_ARITH_COEFF_AB_213_IDX 213

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_214 register. */
#define ATON_ARITH_COEFF_AB_214_IDX 214

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_215 register. */
#define ATON_ARITH_COEFF_AB_215_IDX 215

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_216 register. */
#define ATON_ARITH_COEFF_AB_216_IDX 216

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_217 register. */
#define ATON_ARITH_COEFF_AB_217_IDX 217

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_218 register. */
#define ATON_ARITH_COEFF_AB_218_IDX 218

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_219 register. */
#define ATON_ARITH_COEFF_AB_219_IDX 219

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_220 register. */
#define ATON_ARITH_COEFF_AB_220_IDX 220

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_221 register. */
#define ATON_ARITH_COEFF_AB_221_IDX 221

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_222 register. */
#define ATON_ARITH_COEFF_AB_222_IDX 222

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_223 register. */
#define ATON_ARITH_COEFF_AB_223_IDX 223

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_224 register. */
#define ATON_ARITH_COEFF_AB_224_IDX 224

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_225 register. */
#define ATON_ARITH_COEFF_AB_225_IDX 225

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_226 register. */
#define ATON_ARITH_COEFF_AB_226_IDX 226

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_227 register. */
#define ATON_ARITH_COEFF_AB_227_IDX 227

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_228 register. */
#define ATON_ARITH_COEFF_AB_228_IDX 228

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_229 register. */
#define ATON_ARITH_COEFF_AB_229_IDX 229

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_230 register. */
#define ATON_ARITH_COEFF_AB_230_IDX 230

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_231 register. */
#define ATON_ARITH_COEFF_AB_231_IDX 231

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_232 register. */
#define ATON_ARITH_COEFF_AB_232_IDX 232

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_233 register. */
#define ATON_ARITH_COEFF_AB_233_IDX 233

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_234 register. */
#define ATON_ARITH_COEFF_AB_234_IDX 234

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_235 register. */
#define ATON_ARITH_COEFF_AB_235_IDX 235

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_236 register. */
#define ATON_ARITH_COEFF_AB_236_IDX 236

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_237 register. */
#define ATON_ARITH_COEFF_AB_237_IDX 237

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_238 register. */
#define ATON_ARITH_COEFF_AB_238_IDX 238

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_239 register. */
#define ATON_ARITH_COEFF_AB_239_IDX 239

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_240 register. */
#define ATON_ARITH_COEFF_AB_240_IDX 240

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_241 register. */
#define ATON_ARITH_COEFF_AB_241_IDX 241

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_242 register. */
#define ATON_ARITH_COEFF_AB_242_IDX 242

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_243 register. */
#define ATON_ARITH_COEFF_AB_243_IDX 243

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_244 register. */
#define ATON_ARITH_COEFF_AB_244_IDX 244

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_245 register. */
#define ATON_ARITH_COEFF_AB_245_IDX 245

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_246 register. */
#define ATON_ARITH_COEFF_AB_246_IDX 246

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_247 register. */
#define ATON_ARITH_COEFF_AB_247_IDX 247

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_248 register. */
#define ATON_ARITH_COEFF_AB_248_IDX 248

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_249 register. */
#define ATON_ARITH_COEFF_AB_249_IDX 249

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_250 register. */
#define ATON_ARITH_COEFF_AB_250_IDX 250

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_251 register. */
#define ATON_ARITH_COEFF_AB_251_IDX 251

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_252 register. */
#define ATON_ARITH_COEFF_AB_252_IDX 252

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_253 register. */
#define ATON_ARITH_COEFF_AB_253_IDX 253

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_254 register. */
#define ATON_ARITH_COEFF_AB_254_IDX 254

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_255 register. */
#define ATON_ARITH_COEFF_AB_255_IDX 255

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_256 register. */
#define ATON_ARITH_COEFF_AB_256_IDX 256

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_257 register. */
#define ATON_ARITH_COEFF_AB_257_IDX 257

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_258 register. */
#define ATON_ARITH_COEFF_AB_258_IDX 258

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_259 register. */
#define ATON_ARITH_COEFF_AB_259_IDX 259

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_260 register. */
#define ATON_ARITH_COEFF_AB_260_IDX 260

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_261 register. */
#define ATON_ARITH_COEFF_AB_261_IDX 261

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_262 register. */
#define ATON_ARITH_COEFF_AB_262_IDX 262

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_263 register. */
#define ATON_ARITH_COEFF_AB_263_IDX 263

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_264 register. */
#define ATON_ARITH_COEFF_AB_264_IDX 264

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_265 register. */
#define ATON_ARITH_COEFF_AB_265_IDX 265

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_266 register. */
#define ATON_ARITH_COEFF_AB_266_IDX 266

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_267 register. */
#define ATON_ARITH_COEFF_AB_267_IDX 267

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_268 register. */
#define ATON_ARITH_COEFF_AB_268_IDX 268

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_269 register. */
#define ATON_ARITH_COEFF_AB_269_IDX 269

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_270 register. */
#define ATON_ARITH_COEFF_AB_270_IDX 270

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_271 register. */
#define ATON_ARITH_COEFF_AB_271_IDX 271

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_272 register. */
#define ATON_ARITH_COEFF_AB_272_IDX 272

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_273 register. */
#define ATON_ARITH_COEFF_AB_273_IDX 273

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_274 register. */
#define ATON_ARITH_COEFF_AB_274_IDX 274

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_275 register. */
#define ATON_ARITH_COEFF_AB_275_IDX 275

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_276 register. */
#define ATON_ARITH_COEFF_AB_276_IDX 276

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_277 register. */
#define ATON_ARITH_COEFF_AB_277_IDX 277

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_278 register. */
#define ATON_ARITH_COEFF_AB_278_IDX 278

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_279 register. */
#define ATON_ARITH_COEFF_AB_279_IDX 279

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_280 register. */
#define ATON_ARITH_COEFF_AB_280_IDX 280

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_281 register. */
#define ATON_ARITH_COEFF_AB_281_IDX 281

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_282 register. */
#define ATON_ARITH_COEFF_AB_282_IDX 282

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_283 register. */
#define ATON_ARITH_COEFF_AB_283_IDX 283

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_284 register. */
#define ATON_ARITH_COEFF_AB_284_IDX 284

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_285 register. */
#define ATON_ARITH_COEFF_AB_285_IDX 285

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_286 register. */
#define ATON_ARITH_COEFF_AB_286_IDX 286

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_287 register. */
#define ATON_ARITH_COEFF_AB_287_IDX 287

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_288 register. */
#define ATON_ARITH_COEFF_AB_288_IDX 288

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_289 register. */
#define ATON_ARITH_COEFF_AB_289_IDX 289

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_290 register. */
#define ATON_ARITH_COEFF_AB_290_IDX 290

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_291 register. */
#define ATON_ARITH_COEFF_AB_291_IDX 291

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_292 register. */
#define ATON_ARITH_COEFF_AB_292_IDX 292

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_293 register. */
#define ATON_ARITH_COEFF_AB_293_IDX 293

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_294 register. */
#define ATON_ARITH_COEFF_AB_294_IDX 294

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_295 register. */
#define ATON_ARITH_COEFF_AB_295_IDX 295

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_296 register. */
#define ATON_ARITH_COEFF_AB_296_IDX 296

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_297 register. */
#define ATON_ARITH_COEFF_AB_297_IDX 297

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_298 register. */
#define ATON_ARITH_COEFF_AB_298_IDX 298

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_299 register. */
#define ATON_ARITH_COEFF_AB_299_IDX 299

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_300 register. */
#define ATON_ARITH_COEFF_AB_300_IDX 300

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_301 register. */
#define ATON_ARITH_COEFF_AB_301_IDX 301

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_302 register. */
#define ATON_ARITH_COEFF_AB_302_IDX 302

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_303 register. */
#define ATON_ARITH_COEFF_AB_303_IDX 303

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_304 register. */
#define ATON_ARITH_COEFF_AB_304_IDX 304

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_305 register. */
#define ATON_ARITH_COEFF_AB_305_IDX 305

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_306 register. */
#define ATON_ARITH_COEFF_AB_306_IDX 306

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_307 register. */
#define ATON_ARITH_COEFF_AB_307_IDX 307

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_308 register. */
#define ATON_ARITH_COEFF_AB_308_IDX 308

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_309 register. */
#define ATON_ARITH_COEFF_AB_309_IDX 309

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_310 register. */
#define ATON_ARITH_COEFF_AB_310_IDX 310

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_311 register. */
#define ATON_ARITH_COEFF_AB_311_IDX 311

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_312 register. */
#define ATON_ARITH_COEFF_AB_312_IDX 312

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_313 register. */
#define ATON_ARITH_COEFF_AB_313_IDX 313

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_314 register. */
#define ATON_ARITH_COEFF_AB_314_IDX 314

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_315 register. */
#define ATON_ARITH_COEFF_AB_315_IDX 315

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_316 register. */
#define ATON_ARITH_COEFF_AB_316_IDX 316

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_317 register. */
#define ATON_ARITH_COEFF_AB_317_IDX 317

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_318 register. */
#define ATON_ARITH_COEFF_AB_318_IDX 318

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_319 register. */
#define ATON_ARITH_COEFF_AB_319_IDX 319

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_320 register. */
#define ATON_ARITH_COEFF_AB_320_IDX 320

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_321 register. */
#define ATON_ARITH_COEFF_AB_321_IDX 321

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_322 register. */
#define ATON_ARITH_COEFF_AB_322_IDX 322

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_323 register. */
#define ATON_ARITH_COEFF_AB_323_IDX 323

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_324 register. */
#define ATON_ARITH_COEFF_AB_324_IDX 324

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_325 register. */
#define ATON_ARITH_COEFF_AB_325_IDX 325

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_326 register. */
#define ATON_ARITH_COEFF_AB_326_IDX 326

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_327 register. */
#define ATON_ARITH_COEFF_AB_327_IDX 327

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_328 register. */
#define ATON_ARITH_COEFF_AB_328_IDX 328

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_329 register. */
#define ATON_ARITH_COEFF_AB_329_IDX 329

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_330 register. */
#define ATON_ARITH_COEFF_AB_330_IDX 330

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_331 register. */
#define ATON_ARITH_COEFF_AB_331_IDX 331

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_332 register. */
#define ATON_ARITH_COEFF_AB_332_IDX 332

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_333 register. */
#define ATON_ARITH_COEFF_AB_333_IDX 333

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_334 register. */
#define ATON_ARITH_COEFF_AB_334_IDX 334

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_335 register. */
#define ATON_ARITH_COEFF_AB_335_IDX 335

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_336 register. */
#define ATON_ARITH_COEFF_AB_336_IDX 336

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_337 register. */
#define ATON_ARITH_COEFF_AB_337_IDX 337

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_338 register. */
#define ATON_ARITH_COEFF_AB_338_IDX 338

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_339 register. */
#define ATON_ARITH_COEFF_AB_339_IDX 339

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_340 register. */
#define ATON_ARITH_COEFF_AB_340_IDX 340

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_341 register. */
#define ATON_ARITH_COEFF_AB_341_IDX 341

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_342 register. */
#define ATON_ARITH_COEFF_AB_342_IDX 342

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_343 register. */
#define ATON_ARITH_COEFF_AB_343_IDX 343

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_344 register. */
#define ATON_ARITH_COEFF_AB_344_IDX 344

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_345 register. */
#define ATON_ARITH_COEFF_AB_345_IDX 345

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_346 register. */
#define ATON_ARITH_COEFF_AB_346_IDX 346

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_347 register. */
#define ATON_ARITH_COEFF_AB_347_IDX 347

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_348 register. */
#define ATON_ARITH_COEFF_AB_348_IDX 348

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_349 register. */
#define ATON_ARITH_COEFF_AB_349_IDX 349

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_350 register. */
#define ATON_ARITH_COEFF_AB_350_IDX 350

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_351 register. */
#define ATON_ARITH_COEFF_AB_351_IDX 351

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_352 register. */
#define ATON_ARITH_COEFF_AB_352_IDX 352

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_353 register. */
#define ATON_ARITH_COEFF_AB_353_IDX 353

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_354 register. */
#define ATON_ARITH_COEFF_AB_354_IDX 354

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_355 register. */
#define ATON_ARITH_COEFF_AB_355_IDX 355

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_356 register. */
#define ATON_ARITH_COEFF_AB_356_IDX 356

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_357 register. */
#define ATON_ARITH_COEFF_AB_357_IDX 357

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_358 register. */
#define ATON_ARITH_COEFF_AB_358_IDX 358

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_359 register. */
#define ATON_ARITH_COEFF_AB_359_IDX 359

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_360 register. */
#define ATON_ARITH_COEFF_AB_360_IDX 360

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_361 register. */
#define ATON_ARITH_COEFF_AB_361_IDX 361

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_362 register. */
#define ATON_ARITH_COEFF_AB_362_IDX 362

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_363 register. */
#define ATON_ARITH_COEFF_AB_363_IDX 363

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_364 register. */
#define ATON_ARITH_COEFF_AB_364_IDX 364

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_365 register. */
#define ATON_ARITH_COEFF_AB_365_IDX 365

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_366 register. */
#define ATON_ARITH_COEFF_AB_366_IDX 366

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_367 register. */
#define ATON_ARITH_COEFF_AB_367_IDX 367

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_368 register. */
#define ATON_ARITH_COEFF_AB_368_IDX 368

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_369 register. */
#define ATON_ARITH_COEFF_AB_369_IDX 369

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_370 register. */
#define ATON_ARITH_COEFF_AB_370_IDX 370

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_371 register. */
#define ATON_ARITH_COEFF_AB_371_IDX 371

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_372 register. */
#define ATON_ARITH_COEFF_AB_372_IDX 372

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_373 register. */
#define ATON_ARITH_COEFF_AB_373_IDX 373

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_374 register. */
#define ATON_ARITH_COEFF_AB_374_IDX 374

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_375 register. */
#define ATON_ARITH_COEFF_AB_375_IDX 375

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_376 register. */
#define ATON_ARITH_COEFF_AB_376_IDX 376

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_377 register. */
#define ATON_ARITH_COEFF_AB_377_IDX 377

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_378 register. */
#define ATON_ARITH_COEFF_AB_378_IDX 378

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_379 register. */
#define ATON_ARITH_COEFF_AB_379_IDX 379

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_380 register. */
#define ATON_ARITH_COEFF_AB_380_IDX 380

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_381 register. */
#define ATON_ARITH_COEFF_AB_381_IDX 381

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_382 register. */
#define ATON_ARITH_COEFF_AB_382_IDX 382

/** Value of idx index (expansion index) necessary for obtaining the COEFF_AB_383 register. */
#define ATON_ARITH_COEFF_AB_383_IDX 383


/* *************************************************************************** COEFF_C register of one of the ARITH units *************************************************************************** */

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_0 register. */
#define ATON_ARITH_COEFF_C_0_IDX 0

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_1 register. */
#define ATON_ARITH_COEFF_C_1_IDX 1

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_2 register. */
#define ATON_ARITH_COEFF_C_2_IDX 2

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_3 register. */
#define ATON_ARITH_COEFF_C_3_IDX 3

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_4 register. */
#define ATON_ARITH_COEFF_C_4_IDX 4

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_5 register. */
#define ATON_ARITH_COEFF_C_5_IDX 5

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_6 register. */
#define ATON_ARITH_COEFF_C_6_IDX 6

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_7 register. */
#define ATON_ARITH_COEFF_C_7_IDX 7

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_8 register. */
#define ATON_ARITH_COEFF_C_8_IDX 8

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_9 register. */
#define ATON_ARITH_COEFF_C_9_IDX 9

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_10 register. */
#define ATON_ARITH_COEFF_C_10_IDX 10

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_11 register. */
#define ATON_ARITH_COEFF_C_11_IDX 11

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_12 register. */
#define ATON_ARITH_COEFF_C_12_IDX 12

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_13 register. */
#define ATON_ARITH_COEFF_C_13_IDX 13

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_14 register. */
#define ATON_ARITH_COEFF_C_14_IDX 14

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_15 register. */
#define ATON_ARITH_COEFF_C_15_IDX 15

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_16 register. */
#define ATON_ARITH_COEFF_C_16_IDX 16

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_17 register. */
#define ATON_ARITH_COEFF_C_17_IDX 17

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_18 register. */
#define ATON_ARITH_COEFF_C_18_IDX 18

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_19 register. */
#define ATON_ARITH_COEFF_C_19_IDX 19

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_20 register. */
#define ATON_ARITH_COEFF_C_20_IDX 20

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_21 register. */
#define ATON_ARITH_COEFF_C_21_IDX 21

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_22 register. */
#define ATON_ARITH_COEFF_C_22_IDX 22

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_23 register. */
#define ATON_ARITH_COEFF_C_23_IDX 23

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_24 register. */
#define ATON_ARITH_COEFF_C_24_IDX 24

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_25 register. */
#define ATON_ARITH_COEFF_C_25_IDX 25

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_26 register. */
#define ATON_ARITH_COEFF_C_26_IDX 26

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_27 register. */
#define ATON_ARITH_COEFF_C_27_IDX 27

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_28 register. */
#define ATON_ARITH_COEFF_C_28_IDX 28

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_29 register. */
#define ATON_ARITH_COEFF_C_29_IDX 29

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_30 register. */
#define ATON_ARITH_COEFF_C_30_IDX 30

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_31 register. */
#define ATON_ARITH_COEFF_C_31_IDX 31

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_32 register. */
#define ATON_ARITH_COEFF_C_32_IDX 32

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_33 register. */
#define ATON_ARITH_COEFF_C_33_IDX 33

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_34 register. */
#define ATON_ARITH_COEFF_C_34_IDX 34

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_35 register. */
#define ATON_ARITH_COEFF_C_35_IDX 35

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_36 register. */
#define ATON_ARITH_COEFF_C_36_IDX 36

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_37 register. */
#define ATON_ARITH_COEFF_C_37_IDX 37

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_38 register. */
#define ATON_ARITH_COEFF_C_38_IDX 38

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_39 register. */
#define ATON_ARITH_COEFF_C_39_IDX 39

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_40 register. */
#define ATON_ARITH_COEFF_C_40_IDX 40

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_41 register. */
#define ATON_ARITH_COEFF_C_41_IDX 41

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_42 register. */
#define ATON_ARITH_COEFF_C_42_IDX 42

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_43 register. */
#define ATON_ARITH_COEFF_C_43_IDX 43

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_44 register. */
#define ATON_ARITH_COEFF_C_44_IDX 44

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_45 register. */
#define ATON_ARITH_COEFF_C_45_IDX 45

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_46 register. */
#define ATON_ARITH_COEFF_C_46_IDX 46

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_47 register. */
#define ATON_ARITH_COEFF_C_47_IDX 47

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_48 register. */
#define ATON_ARITH_COEFF_C_48_IDX 48

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_49 register. */
#define ATON_ARITH_COEFF_C_49_IDX 49

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_50 register. */
#define ATON_ARITH_COEFF_C_50_IDX 50

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_51 register. */
#define ATON_ARITH_COEFF_C_51_IDX 51

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_52 register. */
#define ATON_ARITH_COEFF_C_52_IDX 52

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_53 register. */
#define ATON_ARITH_COEFF_C_53_IDX 53

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_54 register. */
#define ATON_ARITH_COEFF_C_54_IDX 54

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_55 register. */
#define ATON_ARITH_COEFF_C_55_IDX 55

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_56 register. */
#define ATON_ARITH_COEFF_C_56_IDX 56

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_57 register. */
#define ATON_ARITH_COEFF_C_57_IDX 57

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_58 register. */
#define ATON_ARITH_COEFF_C_58_IDX 58

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_59 register. */
#define ATON_ARITH_COEFF_C_59_IDX 59

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_60 register. */
#define ATON_ARITH_COEFF_C_60_IDX 60

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_61 register. */
#define ATON_ARITH_COEFF_C_61_IDX 61

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_62 register. */
#define ATON_ARITH_COEFF_C_62_IDX 62

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_63 register. */
#define ATON_ARITH_COEFF_C_63_IDX 63

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_64 register. */
#define ATON_ARITH_COEFF_C_64_IDX 64

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_65 register. */
#define ATON_ARITH_COEFF_C_65_IDX 65

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_66 register. */
#define ATON_ARITH_COEFF_C_66_IDX 66

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_67 register. */
#define ATON_ARITH_COEFF_C_67_IDX 67

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_68 register. */
#define ATON_ARITH_COEFF_C_68_IDX 68

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_69 register. */
#define ATON_ARITH_COEFF_C_69_IDX 69

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_70 register. */
#define ATON_ARITH_COEFF_C_70_IDX 70

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_71 register. */
#define ATON_ARITH_COEFF_C_71_IDX 71

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_72 register. */
#define ATON_ARITH_COEFF_C_72_IDX 72

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_73 register. */
#define ATON_ARITH_COEFF_C_73_IDX 73

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_74 register. */
#define ATON_ARITH_COEFF_C_74_IDX 74

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_75 register. */
#define ATON_ARITH_COEFF_C_75_IDX 75

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_76 register. */
#define ATON_ARITH_COEFF_C_76_IDX 76

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_77 register. */
#define ATON_ARITH_COEFF_C_77_IDX 77

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_78 register. */
#define ATON_ARITH_COEFF_C_78_IDX 78

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_79 register. */
#define ATON_ARITH_COEFF_C_79_IDX 79

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_80 register. */
#define ATON_ARITH_COEFF_C_80_IDX 80

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_81 register. */
#define ATON_ARITH_COEFF_C_81_IDX 81

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_82 register. */
#define ATON_ARITH_COEFF_C_82_IDX 82

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_83 register. */
#define ATON_ARITH_COEFF_C_83_IDX 83

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_84 register. */
#define ATON_ARITH_COEFF_C_84_IDX 84

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_85 register. */
#define ATON_ARITH_COEFF_C_85_IDX 85

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_86 register. */
#define ATON_ARITH_COEFF_C_86_IDX 86

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_87 register. */
#define ATON_ARITH_COEFF_C_87_IDX 87

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_88 register. */
#define ATON_ARITH_COEFF_C_88_IDX 88

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_89 register. */
#define ATON_ARITH_COEFF_C_89_IDX 89

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_90 register. */
#define ATON_ARITH_COEFF_C_90_IDX 90

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_91 register. */
#define ATON_ARITH_COEFF_C_91_IDX 91

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_92 register. */
#define ATON_ARITH_COEFF_C_92_IDX 92

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_93 register. */
#define ATON_ARITH_COEFF_C_93_IDX 93

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_94 register. */
#define ATON_ARITH_COEFF_C_94_IDX 94

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_95 register. */
#define ATON_ARITH_COEFF_C_95_IDX 95

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_96 register. */
#define ATON_ARITH_COEFF_C_96_IDX 96

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_97 register. */
#define ATON_ARITH_COEFF_C_97_IDX 97

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_98 register. */
#define ATON_ARITH_COEFF_C_98_IDX 98

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_99 register. */
#define ATON_ARITH_COEFF_C_99_IDX 99

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_100 register. */
#define ATON_ARITH_COEFF_C_100_IDX 100

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_101 register. */
#define ATON_ARITH_COEFF_C_101_IDX 101

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_102 register. */
#define ATON_ARITH_COEFF_C_102_IDX 102

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_103 register. */
#define ATON_ARITH_COEFF_C_103_IDX 103

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_104 register. */
#define ATON_ARITH_COEFF_C_104_IDX 104

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_105 register. */
#define ATON_ARITH_COEFF_C_105_IDX 105

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_106 register. */
#define ATON_ARITH_COEFF_C_106_IDX 106

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_107 register. */
#define ATON_ARITH_COEFF_C_107_IDX 107

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_108 register. */
#define ATON_ARITH_COEFF_C_108_IDX 108

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_109 register. */
#define ATON_ARITH_COEFF_C_109_IDX 109

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_110 register. */
#define ATON_ARITH_COEFF_C_110_IDX 110

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_111 register. */
#define ATON_ARITH_COEFF_C_111_IDX 111

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_112 register. */
#define ATON_ARITH_COEFF_C_112_IDX 112

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_113 register. */
#define ATON_ARITH_COEFF_C_113_IDX 113

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_114 register. */
#define ATON_ARITH_COEFF_C_114_IDX 114

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_115 register. */
#define ATON_ARITH_COEFF_C_115_IDX 115

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_116 register. */
#define ATON_ARITH_COEFF_C_116_IDX 116

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_117 register. */
#define ATON_ARITH_COEFF_C_117_IDX 117

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_118 register. */
#define ATON_ARITH_COEFF_C_118_IDX 118

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_119 register. */
#define ATON_ARITH_COEFF_C_119_IDX 119

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_120 register. */
#define ATON_ARITH_COEFF_C_120_IDX 120

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_121 register. */
#define ATON_ARITH_COEFF_C_121_IDX 121

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_122 register. */
#define ATON_ARITH_COEFF_C_122_IDX 122

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_123 register. */
#define ATON_ARITH_COEFF_C_123_IDX 123

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_124 register. */
#define ATON_ARITH_COEFF_C_124_IDX 124

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_125 register. */
#define ATON_ARITH_COEFF_C_125_IDX 125

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_126 register. */
#define ATON_ARITH_COEFF_C_126_IDX 126

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_127 register. */
#define ATON_ARITH_COEFF_C_127_IDX 127

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_128 register. */
#define ATON_ARITH_COEFF_C_128_IDX 128

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_129 register. */
#define ATON_ARITH_COEFF_C_129_IDX 129

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_130 register. */
#define ATON_ARITH_COEFF_C_130_IDX 130

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_131 register. */
#define ATON_ARITH_COEFF_C_131_IDX 131

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_132 register. */
#define ATON_ARITH_COEFF_C_132_IDX 132

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_133 register. */
#define ATON_ARITH_COEFF_C_133_IDX 133

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_134 register. */
#define ATON_ARITH_COEFF_C_134_IDX 134

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_135 register. */
#define ATON_ARITH_COEFF_C_135_IDX 135

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_136 register. */
#define ATON_ARITH_COEFF_C_136_IDX 136

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_137 register. */
#define ATON_ARITH_COEFF_C_137_IDX 137

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_138 register. */
#define ATON_ARITH_COEFF_C_138_IDX 138

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_139 register. */
#define ATON_ARITH_COEFF_C_139_IDX 139

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_140 register. */
#define ATON_ARITH_COEFF_C_140_IDX 140

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_141 register. */
#define ATON_ARITH_COEFF_C_141_IDX 141

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_142 register. */
#define ATON_ARITH_COEFF_C_142_IDX 142

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_143 register. */
#define ATON_ARITH_COEFF_C_143_IDX 143

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_144 register. */
#define ATON_ARITH_COEFF_C_144_IDX 144

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_145 register. */
#define ATON_ARITH_COEFF_C_145_IDX 145

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_146 register. */
#define ATON_ARITH_COEFF_C_146_IDX 146

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_147 register. */
#define ATON_ARITH_COEFF_C_147_IDX 147

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_148 register. */
#define ATON_ARITH_COEFF_C_148_IDX 148

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_149 register. */
#define ATON_ARITH_COEFF_C_149_IDX 149

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_150 register. */
#define ATON_ARITH_COEFF_C_150_IDX 150

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_151 register. */
#define ATON_ARITH_COEFF_C_151_IDX 151

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_152 register. */
#define ATON_ARITH_COEFF_C_152_IDX 152

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_153 register. */
#define ATON_ARITH_COEFF_C_153_IDX 153

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_154 register. */
#define ATON_ARITH_COEFF_C_154_IDX 154

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_155 register. */
#define ATON_ARITH_COEFF_C_155_IDX 155

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_156 register. */
#define ATON_ARITH_COEFF_C_156_IDX 156

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_157 register. */
#define ATON_ARITH_COEFF_C_157_IDX 157

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_158 register. */
#define ATON_ARITH_COEFF_C_158_IDX 158

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_159 register. */
#define ATON_ARITH_COEFF_C_159_IDX 159

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_160 register. */
#define ATON_ARITH_COEFF_C_160_IDX 160

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_161 register. */
#define ATON_ARITH_COEFF_C_161_IDX 161

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_162 register. */
#define ATON_ARITH_COEFF_C_162_IDX 162

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_163 register. */
#define ATON_ARITH_COEFF_C_163_IDX 163

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_164 register. */
#define ATON_ARITH_COEFF_C_164_IDX 164

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_165 register. */
#define ATON_ARITH_COEFF_C_165_IDX 165

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_166 register. */
#define ATON_ARITH_COEFF_C_166_IDX 166

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_167 register. */
#define ATON_ARITH_COEFF_C_167_IDX 167

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_168 register. */
#define ATON_ARITH_COEFF_C_168_IDX 168

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_169 register. */
#define ATON_ARITH_COEFF_C_169_IDX 169

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_170 register. */
#define ATON_ARITH_COEFF_C_170_IDX 170

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_171 register. */
#define ATON_ARITH_COEFF_C_171_IDX 171

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_172 register. */
#define ATON_ARITH_COEFF_C_172_IDX 172

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_173 register. */
#define ATON_ARITH_COEFF_C_173_IDX 173

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_174 register. */
#define ATON_ARITH_COEFF_C_174_IDX 174

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_175 register. */
#define ATON_ARITH_COEFF_C_175_IDX 175

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_176 register. */
#define ATON_ARITH_COEFF_C_176_IDX 176

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_177 register. */
#define ATON_ARITH_COEFF_C_177_IDX 177

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_178 register. */
#define ATON_ARITH_COEFF_C_178_IDX 178

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_179 register. */
#define ATON_ARITH_COEFF_C_179_IDX 179

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_180 register. */
#define ATON_ARITH_COEFF_C_180_IDX 180

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_181 register. */
#define ATON_ARITH_COEFF_C_181_IDX 181

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_182 register. */
#define ATON_ARITH_COEFF_C_182_IDX 182

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_183 register. */
#define ATON_ARITH_COEFF_C_183_IDX 183

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_184 register. */
#define ATON_ARITH_COEFF_C_184_IDX 184

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_185 register. */
#define ATON_ARITH_COEFF_C_185_IDX 185

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_186 register. */
#define ATON_ARITH_COEFF_C_186_IDX 186

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_187 register. */
#define ATON_ARITH_COEFF_C_187_IDX 187

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_188 register. */
#define ATON_ARITH_COEFF_C_188_IDX 188

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_189 register. */
#define ATON_ARITH_COEFF_C_189_IDX 189

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_190 register. */
#define ATON_ARITH_COEFF_C_190_IDX 190

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_191 register. */
#define ATON_ARITH_COEFF_C_191_IDX 191

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_192 register. */
#define ATON_ARITH_COEFF_C_192_IDX 192

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_193 register. */
#define ATON_ARITH_COEFF_C_193_IDX 193

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_194 register. */
#define ATON_ARITH_COEFF_C_194_IDX 194

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_195 register. */
#define ATON_ARITH_COEFF_C_195_IDX 195

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_196 register. */
#define ATON_ARITH_COEFF_C_196_IDX 196

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_197 register. */
#define ATON_ARITH_COEFF_C_197_IDX 197

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_198 register. */
#define ATON_ARITH_COEFF_C_198_IDX 198

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_199 register. */
#define ATON_ARITH_COEFF_C_199_IDX 199

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_200 register. */
#define ATON_ARITH_COEFF_C_200_IDX 200

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_201 register. */
#define ATON_ARITH_COEFF_C_201_IDX 201

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_202 register. */
#define ATON_ARITH_COEFF_C_202_IDX 202

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_203 register. */
#define ATON_ARITH_COEFF_C_203_IDX 203

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_204 register. */
#define ATON_ARITH_COEFF_C_204_IDX 204

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_205 register. */
#define ATON_ARITH_COEFF_C_205_IDX 205

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_206 register. */
#define ATON_ARITH_COEFF_C_206_IDX 206

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_207 register. */
#define ATON_ARITH_COEFF_C_207_IDX 207

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_208 register. */
#define ATON_ARITH_COEFF_C_208_IDX 208

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_209 register. */
#define ATON_ARITH_COEFF_C_209_IDX 209

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_210 register. */
#define ATON_ARITH_COEFF_C_210_IDX 210

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_211 register. */
#define ATON_ARITH_COEFF_C_211_IDX 211

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_212 register. */
#define ATON_ARITH_COEFF_C_212_IDX 212

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_213 register. */
#define ATON_ARITH_COEFF_C_213_IDX 213

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_214 register. */
#define ATON_ARITH_COEFF_C_214_IDX 214

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_215 register. */
#define ATON_ARITH_COEFF_C_215_IDX 215

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_216 register. */
#define ATON_ARITH_COEFF_C_216_IDX 216

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_217 register. */
#define ATON_ARITH_COEFF_C_217_IDX 217

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_218 register. */
#define ATON_ARITH_COEFF_C_218_IDX 218

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_219 register. */
#define ATON_ARITH_COEFF_C_219_IDX 219

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_220 register. */
#define ATON_ARITH_COEFF_C_220_IDX 220

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_221 register. */
#define ATON_ARITH_COEFF_C_221_IDX 221

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_222 register. */
#define ATON_ARITH_COEFF_C_222_IDX 222

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_223 register. */
#define ATON_ARITH_COEFF_C_223_IDX 223

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_224 register. */
#define ATON_ARITH_COEFF_C_224_IDX 224

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_225 register. */
#define ATON_ARITH_COEFF_C_225_IDX 225

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_226 register. */
#define ATON_ARITH_COEFF_C_226_IDX 226

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_227 register. */
#define ATON_ARITH_COEFF_C_227_IDX 227

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_228 register. */
#define ATON_ARITH_COEFF_C_228_IDX 228

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_229 register. */
#define ATON_ARITH_COEFF_C_229_IDX 229

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_230 register. */
#define ATON_ARITH_COEFF_C_230_IDX 230

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_231 register. */
#define ATON_ARITH_COEFF_C_231_IDX 231

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_232 register. */
#define ATON_ARITH_COEFF_C_232_IDX 232

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_233 register. */
#define ATON_ARITH_COEFF_C_233_IDX 233

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_234 register. */
#define ATON_ARITH_COEFF_C_234_IDX 234

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_235 register. */
#define ATON_ARITH_COEFF_C_235_IDX 235

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_236 register. */
#define ATON_ARITH_COEFF_C_236_IDX 236

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_237 register. */
#define ATON_ARITH_COEFF_C_237_IDX 237

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_238 register. */
#define ATON_ARITH_COEFF_C_238_IDX 238

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_239 register. */
#define ATON_ARITH_COEFF_C_239_IDX 239

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_240 register. */
#define ATON_ARITH_COEFF_C_240_IDX 240

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_241 register. */
#define ATON_ARITH_COEFF_C_241_IDX 241

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_242 register. */
#define ATON_ARITH_COEFF_C_242_IDX 242

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_243 register. */
#define ATON_ARITH_COEFF_C_243_IDX 243

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_244 register. */
#define ATON_ARITH_COEFF_C_244_IDX 244

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_245 register. */
#define ATON_ARITH_COEFF_C_245_IDX 245

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_246 register. */
#define ATON_ARITH_COEFF_C_246_IDX 246

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_247 register. */
#define ATON_ARITH_COEFF_C_247_IDX 247

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_248 register. */
#define ATON_ARITH_COEFF_C_248_IDX 248

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_249 register. */
#define ATON_ARITH_COEFF_C_249_IDX 249

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_250 register. */
#define ATON_ARITH_COEFF_C_250_IDX 250

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_251 register. */
#define ATON_ARITH_COEFF_C_251_IDX 251

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_252 register. */
#define ATON_ARITH_COEFF_C_252_IDX 252

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_253 register. */
#define ATON_ARITH_COEFF_C_253_IDX 253

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_254 register. */
#define ATON_ARITH_COEFF_C_254_IDX 254

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_255 register. */
#define ATON_ARITH_COEFF_C_255_IDX 255

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_256 register. */
#define ATON_ARITH_COEFF_C_256_IDX 256

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_257 register. */
#define ATON_ARITH_COEFF_C_257_IDX 257

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_258 register. */
#define ATON_ARITH_COEFF_C_258_IDX 258

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_259 register. */
#define ATON_ARITH_COEFF_C_259_IDX 259

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_260 register. */
#define ATON_ARITH_COEFF_C_260_IDX 260

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_261 register. */
#define ATON_ARITH_COEFF_C_261_IDX 261

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_262 register. */
#define ATON_ARITH_COEFF_C_262_IDX 262

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_263 register. */
#define ATON_ARITH_COEFF_C_263_IDX 263

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_264 register. */
#define ATON_ARITH_COEFF_C_264_IDX 264

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_265 register. */
#define ATON_ARITH_COEFF_C_265_IDX 265

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_266 register. */
#define ATON_ARITH_COEFF_C_266_IDX 266

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_267 register. */
#define ATON_ARITH_COEFF_C_267_IDX 267

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_268 register. */
#define ATON_ARITH_COEFF_C_268_IDX 268

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_269 register. */
#define ATON_ARITH_COEFF_C_269_IDX 269

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_270 register. */
#define ATON_ARITH_COEFF_C_270_IDX 270

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_271 register. */
#define ATON_ARITH_COEFF_C_271_IDX 271

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_272 register. */
#define ATON_ARITH_COEFF_C_272_IDX 272

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_273 register. */
#define ATON_ARITH_COEFF_C_273_IDX 273

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_274 register. */
#define ATON_ARITH_COEFF_C_274_IDX 274

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_275 register. */
#define ATON_ARITH_COEFF_C_275_IDX 275

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_276 register. */
#define ATON_ARITH_COEFF_C_276_IDX 276

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_277 register. */
#define ATON_ARITH_COEFF_C_277_IDX 277

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_278 register. */
#define ATON_ARITH_COEFF_C_278_IDX 278

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_279 register. */
#define ATON_ARITH_COEFF_C_279_IDX 279

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_280 register. */
#define ATON_ARITH_COEFF_C_280_IDX 280

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_281 register. */
#define ATON_ARITH_COEFF_C_281_IDX 281

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_282 register. */
#define ATON_ARITH_COEFF_C_282_IDX 282

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_283 register. */
#define ATON_ARITH_COEFF_C_283_IDX 283

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_284 register. */
#define ATON_ARITH_COEFF_C_284_IDX 284

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_285 register. */
#define ATON_ARITH_COEFF_C_285_IDX 285

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_286 register. */
#define ATON_ARITH_COEFF_C_286_IDX 286

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_287 register. */
#define ATON_ARITH_COEFF_C_287_IDX 287

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_288 register. */
#define ATON_ARITH_COEFF_C_288_IDX 288

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_289 register. */
#define ATON_ARITH_COEFF_C_289_IDX 289

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_290 register. */
#define ATON_ARITH_COEFF_C_290_IDX 290

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_291 register. */
#define ATON_ARITH_COEFF_C_291_IDX 291

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_292 register. */
#define ATON_ARITH_COEFF_C_292_IDX 292

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_293 register. */
#define ATON_ARITH_COEFF_C_293_IDX 293

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_294 register. */
#define ATON_ARITH_COEFF_C_294_IDX 294

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_295 register. */
#define ATON_ARITH_COEFF_C_295_IDX 295

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_296 register. */
#define ATON_ARITH_COEFF_C_296_IDX 296

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_297 register. */
#define ATON_ARITH_COEFF_C_297_IDX 297

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_298 register. */
#define ATON_ARITH_COEFF_C_298_IDX 298

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_299 register. */
#define ATON_ARITH_COEFF_C_299_IDX 299

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_300 register. */
#define ATON_ARITH_COEFF_C_300_IDX 300

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_301 register. */
#define ATON_ARITH_COEFF_C_301_IDX 301

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_302 register. */
#define ATON_ARITH_COEFF_C_302_IDX 302

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_303 register. */
#define ATON_ARITH_COEFF_C_303_IDX 303

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_304 register. */
#define ATON_ARITH_COEFF_C_304_IDX 304

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_305 register. */
#define ATON_ARITH_COEFF_C_305_IDX 305

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_306 register. */
#define ATON_ARITH_COEFF_C_306_IDX 306

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_307 register. */
#define ATON_ARITH_COEFF_C_307_IDX 307

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_308 register. */
#define ATON_ARITH_COEFF_C_308_IDX 308

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_309 register. */
#define ATON_ARITH_COEFF_C_309_IDX 309

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_310 register. */
#define ATON_ARITH_COEFF_C_310_IDX 310

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_311 register. */
#define ATON_ARITH_COEFF_C_311_IDX 311

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_312 register. */
#define ATON_ARITH_COEFF_C_312_IDX 312

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_313 register. */
#define ATON_ARITH_COEFF_C_313_IDX 313

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_314 register. */
#define ATON_ARITH_COEFF_C_314_IDX 314

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_315 register. */
#define ATON_ARITH_COEFF_C_315_IDX 315

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_316 register. */
#define ATON_ARITH_COEFF_C_316_IDX 316

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_317 register. */
#define ATON_ARITH_COEFF_C_317_IDX 317

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_318 register. */
#define ATON_ARITH_COEFF_C_318_IDX 318

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_319 register. */
#define ATON_ARITH_COEFF_C_319_IDX 319

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_320 register. */
#define ATON_ARITH_COEFF_C_320_IDX 320

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_321 register. */
#define ATON_ARITH_COEFF_C_321_IDX 321

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_322 register. */
#define ATON_ARITH_COEFF_C_322_IDX 322

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_323 register. */
#define ATON_ARITH_COEFF_C_323_IDX 323

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_324 register. */
#define ATON_ARITH_COEFF_C_324_IDX 324

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_325 register. */
#define ATON_ARITH_COEFF_C_325_IDX 325

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_326 register. */
#define ATON_ARITH_COEFF_C_326_IDX 326

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_327 register. */
#define ATON_ARITH_COEFF_C_327_IDX 327

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_328 register. */
#define ATON_ARITH_COEFF_C_328_IDX 328

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_329 register. */
#define ATON_ARITH_COEFF_C_329_IDX 329

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_330 register. */
#define ATON_ARITH_COEFF_C_330_IDX 330

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_331 register. */
#define ATON_ARITH_COEFF_C_331_IDX 331

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_332 register. */
#define ATON_ARITH_COEFF_C_332_IDX 332

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_333 register. */
#define ATON_ARITH_COEFF_C_333_IDX 333

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_334 register. */
#define ATON_ARITH_COEFF_C_334_IDX 334

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_335 register. */
#define ATON_ARITH_COEFF_C_335_IDX 335

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_336 register. */
#define ATON_ARITH_COEFF_C_336_IDX 336

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_337 register. */
#define ATON_ARITH_COEFF_C_337_IDX 337

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_338 register. */
#define ATON_ARITH_COEFF_C_338_IDX 338

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_339 register. */
#define ATON_ARITH_COEFF_C_339_IDX 339

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_340 register. */
#define ATON_ARITH_COEFF_C_340_IDX 340

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_341 register. */
#define ATON_ARITH_COEFF_C_341_IDX 341

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_342 register. */
#define ATON_ARITH_COEFF_C_342_IDX 342

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_343 register. */
#define ATON_ARITH_COEFF_C_343_IDX 343

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_344 register. */
#define ATON_ARITH_COEFF_C_344_IDX 344

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_345 register. */
#define ATON_ARITH_COEFF_C_345_IDX 345

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_346 register. */
#define ATON_ARITH_COEFF_C_346_IDX 346

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_347 register. */
#define ATON_ARITH_COEFF_C_347_IDX 347

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_348 register. */
#define ATON_ARITH_COEFF_C_348_IDX 348

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_349 register. */
#define ATON_ARITH_COEFF_C_349_IDX 349

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_350 register. */
#define ATON_ARITH_COEFF_C_350_IDX 350

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_351 register. */
#define ATON_ARITH_COEFF_C_351_IDX 351

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_352 register. */
#define ATON_ARITH_COEFF_C_352_IDX 352

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_353 register. */
#define ATON_ARITH_COEFF_C_353_IDX 353

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_354 register. */
#define ATON_ARITH_COEFF_C_354_IDX 354

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_355 register. */
#define ATON_ARITH_COEFF_C_355_IDX 355

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_356 register. */
#define ATON_ARITH_COEFF_C_356_IDX 356

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_357 register. */
#define ATON_ARITH_COEFF_C_357_IDX 357

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_358 register. */
#define ATON_ARITH_COEFF_C_358_IDX 358

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_359 register. */
#define ATON_ARITH_COEFF_C_359_IDX 359

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_360 register. */
#define ATON_ARITH_COEFF_C_360_IDX 360

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_361 register. */
#define ATON_ARITH_COEFF_C_361_IDX 361

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_362 register. */
#define ATON_ARITH_COEFF_C_362_IDX 362

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_363 register. */
#define ATON_ARITH_COEFF_C_363_IDX 363

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_364 register. */
#define ATON_ARITH_COEFF_C_364_IDX 364

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_365 register. */
#define ATON_ARITH_COEFF_C_365_IDX 365

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_366 register. */
#define ATON_ARITH_COEFF_C_366_IDX 366

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_367 register. */
#define ATON_ARITH_COEFF_C_367_IDX 367

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_368 register. */
#define ATON_ARITH_COEFF_C_368_IDX 368

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_369 register. */
#define ATON_ARITH_COEFF_C_369_IDX 369

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_370 register. */
#define ATON_ARITH_COEFF_C_370_IDX 370

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_371 register. */
#define ATON_ARITH_COEFF_C_371_IDX 371

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_372 register. */
#define ATON_ARITH_COEFF_C_372_IDX 372

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_373 register. */
#define ATON_ARITH_COEFF_C_373_IDX 373

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_374 register. */
#define ATON_ARITH_COEFF_C_374_IDX 374

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_375 register. */
#define ATON_ARITH_COEFF_C_375_IDX 375

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_376 register. */
#define ATON_ARITH_COEFF_C_376_IDX 376

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_377 register. */
#define ATON_ARITH_COEFF_C_377_IDX 377

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_378 register. */
#define ATON_ARITH_COEFF_C_378_IDX 378

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_379 register. */
#define ATON_ARITH_COEFF_C_379_IDX 379

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_380 register. */
#define ATON_ARITH_COEFF_C_380_IDX 380

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_381 register. */
#define ATON_ARITH_COEFF_C_381_IDX 381

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_382 register. */
#define ATON_ARITH_COEFF_C_382_IDX 382

/** Value of idx index (expansion index) necessary for obtaining the COEFF_C_383 register. */
#define ATON_ARITH_COEFF_C_383_IDX 383


/*@}*/


/* ************************************************************************************************************************************************************************************************** */
/*                                                                                                                                                                                                    */
/*                                                                                            BUSIF units                                                                                             */
/*                                                                                                                                                                                                    */
/* ************************************************************************************************************************************************************************************************** */

/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */
/*                                                                                                                                                                                                    */
/*                                                                                            BUSIF units                                                                                             */
/*                                                                                                                                                                                                    */
/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */

/**
 * \name Macros of the BUSIF units
 */
/*@{*/

/* --------------------------------------------------------------------------- KEY_VALID field of the BUSIF.CTRL register --------------------------------------------------------------------------- */

/** Value of idx index (expansion index) necessary for obtaining the KEY0_VALID field of the CTRL register. */
#define ATON_BUSIF_CTRL_KEY_VALID_KEY0_VALID_IDX 0

/** Value of idx index (expansion index) necessary for obtaining the KEY1_VALID field of the CTRL register. */
#define ATON_BUSIF_CTRL_KEY_VALID_KEY1_VALID_IDX 1


/* --------------------------------------------------------------------------- BUSPORT field of the BUSIF.PRIO1 register ---------------------------------------------------------------------------- */

/** Value of idx index (expansion index) for BUSIF_0.PRIO1.BUSPORT0 register field. */
#define ATON_BUSIF_0_PRIO1_BUSPORT0_IDX 0

/** Value of idx index (expansion index) for BUSIF_0.PRIO1.BUSPORT1 register field. */
#define ATON_BUSIF_0_PRIO1_BUSPORT1_IDX 1

/** Value of idx index (expansion index) for BUSIF_0.PRIO1.BUSPORT2 register field. */
#define ATON_BUSIF_0_PRIO1_BUSPORT2_IDX 2

/** Value of idx index (expansion index) for BUSIF_0.PRIO1.BUSPORT3 register field. */
#define ATON_BUSIF_0_PRIO1_BUSPORT3_IDX 3

/** Value of idx index (expansion index) for BUSIF_0.PRIO1.BUSPORT4 register field. */
#define ATON_BUSIF_0_PRIO1_BUSPORT4_IDX 4

/** Value of idx index (expansion index) for BUSIF_0.PRIO1.BUSPORT5 register field. */
#define ATON_BUSIF_0_PRIO1_BUSPORT5_IDX 5

/** Value of idx index (expansion index) for BUSIF_0.PRIO1.BUSPORT6 register field. */
#define ATON_BUSIF_0_PRIO1_BUSPORT6_IDX 6


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT0 field of the PRIO1 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT0 field of the PRIO1 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO1_BUSPORT_BUSPORT0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT1 field of the PRIO1 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT1 field of the PRIO1 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO1_BUSPORT_BUSPORT1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT2 field of the PRIO1 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT2 field of the PRIO1 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO1_BUSPORT_BUSPORT2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT3 field of the PRIO1 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT3 field of the PRIO1 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO1_BUSPORT_BUSPORT3_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT4 field of the PRIO1 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT4 field of the PRIO1 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO1_BUSPORT_BUSPORT4_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT5 field of the PRIO1 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT5 field of the PRIO1 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO1_BUSPORT_BUSPORT5_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT6 field of the PRIO1 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT6 field of the PRIO1 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO1_BUSPORT_BUSPORT6_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)



/* --------------------------------------------------------------------------- BUSPORT field of the BUSIF.PRIO2 register ---------------------------------------------------------------------------- */

/** Value of idx index (expansion index) for BUSIF_0.PRIO2.BUSPORT0 register field. */
#define ATON_BUSIF_0_PRIO2_BUSPORT0_IDX 0

/** Value of idx index (expansion index) for BUSIF_0.PRIO2.BUSPORT1 register field. */
#define ATON_BUSIF_0_PRIO2_BUSPORT1_IDX 1

/** Value of idx index (expansion index) for BUSIF_0.PRIO2.BUSPORT2 register field. */
#define ATON_BUSIF_0_PRIO2_BUSPORT2_IDX 2

/** Value of idx index (expansion index) for BUSIF_0.PRIO2.BUSPORT3 register field. */
#define ATON_BUSIF_0_PRIO2_BUSPORT3_IDX 3

/** Value of idx index (expansion index) for BUSIF_0.PRIO2.BUSPORT4 register field. */
#define ATON_BUSIF_0_PRIO2_BUSPORT4_IDX 4

/** Value of idx index (expansion index) for BUSIF_0.PRIO2.BUSPORT5 register field. */
#define ATON_BUSIF_0_PRIO2_BUSPORT5_IDX 5

/** Value of idx index (expansion index) for BUSIF_0.PRIO2.BUSPORT6 register field. */
#define ATON_BUSIF_0_PRIO2_BUSPORT6_IDX 6


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT0 field of the PRIO2 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT0 field of the PRIO2 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO2_BUSPORT_BUSPORT0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT1 field of the PRIO2 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT1 field of the PRIO2 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO2_BUSPORT_BUSPORT1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT2 field of the PRIO2 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT2 field of the PRIO2 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO2_BUSPORT_BUSPORT2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT3 field of the PRIO2 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT3 field of the PRIO2 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO2_BUSPORT_BUSPORT3_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT4 field of the PRIO2 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT4 field of the PRIO2 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO2_BUSPORT_BUSPORT4_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT5 field of the PRIO2 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT5 field of the PRIO2 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO2_BUSPORT_BUSPORT5_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT6 field of the PRIO2 register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT6 field of the PRIO2 register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_PRIO2_BUSPORT_BUSPORT6_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)



/* ---------------------------------------------------------------------------- BUSPORT field of the BUSIF.ERR register ----------------------------------------------------------------------------- */

/** Value of idx index (expansion index) for BUSIF_0.ERR.BUSPORT0 register field. */
#define ATON_BUSIF_0_ERR_BUSPORT0_IDX 0

/** Value of idx index (expansion index) for BUSIF_0.ERR.BUSPORT1 register field. */
#define ATON_BUSIF_0_ERR_BUSPORT1_IDX 1

/** Value of idx index (expansion index) for BUSIF_0.ERR.BUSPORT2 register field. */
#define ATON_BUSIF_0_ERR_BUSPORT2_IDX 2

/** Value of idx index (expansion index) for BUSIF_0.ERR.BUSPORT3 register field. */
#define ATON_BUSIF_0_ERR_BUSPORT3_IDX 3

/** Value of idx index (expansion index) for BUSIF_0.ERR.BUSPORT4 register field. */
#define ATON_BUSIF_0_ERR_BUSPORT4_IDX 4

/** Value of idx index (expansion index) for BUSIF_0.ERR.BUSPORT5 register field. */
#define ATON_BUSIF_0_ERR_BUSPORT5_IDX 5

/** Value of idx index (expansion index) for BUSIF_0.ERR.BUSPORT6 register field. */
#define ATON_BUSIF_0_ERR_BUSPORT6_IDX 6


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT0 field of the ERR register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT0 field of the ERR register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_ERR_BUSPORT_BUSPORT0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT1 field of the ERR register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT1 field of the ERR register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_ERR_BUSPORT_BUSPORT1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT2 field of the ERR register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT2 field of the ERR register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_ERR_BUSPORT_BUSPORT2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT3 field of the ERR register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT3 field of the ERR register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_ERR_BUSPORT_BUSPORT3_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT4 field of the ERR register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT4 field of the ERR register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_ERR_BUSPORT_BUSPORT4_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT5 field of the ERR register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT5 field of the ERR register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_ERR_BUSPORT_BUSPORT5_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSPORT6 field of the ERR register given the index of one of the BUSIF units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'BUSIF_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSPORT6 field of the ERR register given the index \e UNIT of one of the BUSIF units
 */

#define ATON_BUSIF_ERR_BUSPORT_BUSPORT6_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)



/*@}*/


/* ************************************************************************************************************************************************************************************************** */
/*                                                                                                                                                                                                    */
/*                                                                                           CLKCTRL units                                                                                            */
/*                                                                                                                                                                                                    */
/* ************************************************************************************************************************************************************************************************** */

/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */
/*                                                                                                                                                                                                    */
/*                                                                                           CLKCTRL units                                                                                            */
/*                                                                                                                                                                                                    */
/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */

/**
 * \name Macros of the CLKCTRL units
 */
/*@{*/

/* --------------------------------------------------------------------------- CLK field of the CLKCTRL.AGATES0 register ---------------------------------------------------------------------------- */

/** Value of idx index (expansion index) for CLKCTRL_0.AGATES0.INTCTRL_0 register field. */
#define ATON_CLKCTRL_0_AGATES0_INTCTRL_0_IDX 0

/** Value of idx index (expansion index) for CLKCTRL_0.AGATES0.BUSIF_0 register field. */
#define ATON_CLKCTRL_0_AGATES0_BUSIF_0_IDX 1

/** Value of idx index (expansion index) for CLKCTRL_0.AGATES0.STRSWITCH_0 register field. */
#define ATON_CLKCTRL_0_AGATES0_STRSWITCH_0_IDX 2


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the AGATES0 register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the AGATES0 register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_AGATES0_CLK_BUSIF_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the INTCTRL_0 field of the AGATES0 register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the INTCTRL_0 field of the AGATES0 register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_AGATES0_CLK_INTCTRL_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the AGATES0 register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the AGATES0 register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_AGATES0_CLK_STRSWITCH_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)



/* ************************************************************************** AGATES1 register of one of the CLKCTRL units ************************************************************************** */


/* --------------------------------------------------------------------------- CLK field of the CLKCTRL.AGATES1 register ---------------------------------------------------------------------------- */

/** Value of idx index (expansion index) for CLKCTRL_0.AGATES1.INTCTRL_0 register field. */
#define ATON_CLKCTRL_0_AGATES1_INTCTRL_0_IDX 0

/** Value of idx index (expansion index) for CLKCTRL_0.AGATES1.BUSIF_0 register field. */
#define ATON_CLKCTRL_0_AGATES1_BUSIF_0_IDX 1

/** Value of idx index (expansion index) for CLKCTRL_0.AGATES1.STRSWITCH_0 register field. */
#define ATON_CLKCTRL_0_AGATES1_STRSWITCH_0_IDX 2


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the AGATES1 register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the AGATES1 register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_AGATES1_CLK_BUSIF_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the INTCTRL_0 field of the AGATES1 register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the INTCTRL_0 field of the AGATES1 register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_AGATES1_CLK_INTCTRL_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the AGATES1 register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the AGATES1 register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_AGATES1_CLK_STRSWITCH_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)



/* ---------------------------------------------------------------------------- CLK field of the CLKCTRL.BGATES register ---------------------------------------------------------------------------- */

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.STRENG_0 register field. */
#define ATON_CLKCTRL_0_BGATES_STRENG_0_IDX 0

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.STRENG_1 register field. */
#define ATON_CLKCTRL_0_BGATES_STRENG_1_IDX 1

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.STRENG_2 register field. */
#define ATON_CLKCTRL_0_BGATES_STRENG_2_IDX 2

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.STRENG_3 register field. */
#define ATON_CLKCTRL_0_BGATES_STRENG_3_IDX 3

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.STRENG_4 register field. */
#define ATON_CLKCTRL_0_BGATES_STRENG_4_IDX 4

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.STRENG_5 register field. */
#define ATON_CLKCTRL_0_BGATES_STRENG_5_IDX 5

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.CONVACC_0 register field. */
#define ATON_CLKCTRL_0_BGATES_CONVACC_0_IDX 6

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.ACTIV_0 register field. */
#define ATON_CLKCTRL_0_BGATES_ACTIV_0_IDX 7

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.ARITH_0 register field. */
#define ATON_CLKCTRL_0_BGATES_ARITH_0_IDX 8

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.ARITH_1 register field. */
#define ATON_CLKCTRL_0_BGATES_ARITH_1_IDX 9

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.POOL_0 register field. */
#define ATON_CLKCTRL_0_BGATES_POOL_0_IDX 10

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.EPOCHCTRL_0 register field. */
#define ATON_CLKCTRL_0_BGATES_EPOCHCTRL_0_IDX 11

/** Value of idx index (expansion index) for CLKCTRL_0.BGATES.DEBUG_TRACE_0 register field. */
#define ATON_CLKCTRL_0_BGATES_DEBUG_TRACE_0_IDX 12


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ACTIV_0 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ACTIV_0 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_ACTIV_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 7 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ARITH_0 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ARITH_0 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_ARITH_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 8 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ARITH_1 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ARITH_1 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_ARITH_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 9 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the CONVACC_0 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the CONVACC_0 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_CONVACC_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DEBUG_TRACE_0 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DEBUG_TRACE_0 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_DEBUG_TRACE_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 12 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_EPOCHCTRL_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 11 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the POOL_0 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the POOL_0 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_POOL_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 10 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_0 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_0 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_STRENG_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_1 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_1 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_STRENG_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_2 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_2 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_STRENG_2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_3 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_3 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_STRENG_3_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_4 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_4 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_STRENG_4_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_5 field of the BGATES register given the index of one of the CLKCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'CLKCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_5 field of the BGATES register given the index \e UNIT of one of the CLKCTRL units
 */

#define ATON_CLKCTRL_BGATES_CLK_STRENG_5_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)



/*@}*/


/* ************************************************************************************************************************************************************************************************** */
/*                                                                                                                                                                                                    */
/*                                                                                           CONVACC units                                                                                            */
/*                                                                                                                                                                                                    */
/* ************************************************************************************************************************************************************************************************** */

/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */
/*                                                                                                                                                                                                    */
/*                                                                                           CONVACC units                                                                                            */
/*                                                                                                                                                                                                    */
/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */

/**
 * \name Macros of the CONVACC units
 */
/*@{*/

/*@}*/


/* ************************************************************************************************************************************************************************************************** */
/*                                                                                                                                                                                                    */
/*                                                                                         DEBUG_TRACE units                                                                                          */
/*                                                                                                                                                                                                    */
/* ************************************************************************************************************************************************************************************************** */

/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */
/*                                                                                                                                                                                                    */
/*                                                                                         DEBUG_TRACE units                                                                                          */
/*                                                                                                                                                                                                    */
/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */

/**
 * \name Macros of the DEBUG_TRACE units
 */
/*@{*/

/* ********************************************************************* ILINK_STATES register of one of the DEBUG_TRACE units ********************************************************************** */

/** Value of idx index (expansion index) for DEBUG_TRACE_0.ILINK_STATES0 register. */
#define ATON_DEBUG_TRACE_0_ILINK_STATES0_IDX 0


/**
 * Return the value of idx index (expansion index) necessary for obtaining the ILINK_STATES0 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the ILINK_STATES0 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_ILINK_STATES_ILINK_STATES0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)



/* ********************************************************************* OLINK_STATES register of one of the DEBUG_TRACE units ********************************************************************** */

/** Value of idx index (expansion index) for DEBUG_TRACE_0.OLINK_STATES0 register. */
#define ATON_DEBUG_TRACE_0_OLINK_STATES0_IDX 0


/**
 * Return the value of idx index (expansion index) necessary for obtaining the OLINK_STATES0 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the OLINK_STATES0 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_OLINK_STATES_OLINK_STATES0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)



/* ************************************************************************* TRIG register of one of the DEBUG_TRACE units ************************************************************************** */

/** Value of idx index (expansion index) for DEBUG_TRACE_0.TRIG_0 register. */
#define ATON_DEBUG_TRACE_0_TRIG_0_IDX 0

/** Value of idx index (expansion index) for DEBUG_TRACE_0.TRIG_1 register. */
#define ATON_DEBUG_TRACE_0_TRIG_1_IDX 1

/** Value of idx index (expansion index) for DEBUG_TRACE_0.TRIG_2 register. */
#define ATON_DEBUG_TRACE_0_TRIG_2_IDX 2

/** Value of idx index (expansion index) for DEBUG_TRACE_0.TRIG_3 register. */
#define ATON_DEBUG_TRACE_0_TRIG_3_IDX 3


/**
 * Return the value of idx index (expansion index) necessary for obtaining the TRIG_0 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the TRIG_0 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_TRIG_TRIG_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the TRIG_1 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the TRIG_1 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_TRIG_TRIG_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the TRIG_2 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the TRIG_2 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_TRIG_TRIG_2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the TRIG_3 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the TRIG_3 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_TRIG_TRIG_3_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)



/* ************************************************************************* EVENT register of one of the DEBUG_TRACE units ************************************************************************* */

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_0 register. */
#define ATON_DEBUG_TRACE_0_EVENT_0_IDX 0

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_1 register. */
#define ATON_DEBUG_TRACE_0_EVENT_1_IDX 1

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_2 register. */
#define ATON_DEBUG_TRACE_0_EVENT_2_IDX 2

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_3 register. */
#define ATON_DEBUG_TRACE_0_EVENT_3_IDX 3

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_4 register. */
#define ATON_DEBUG_TRACE_0_EVENT_4_IDX 4

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_5 register. */
#define ATON_DEBUG_TRACE_0_EVENT_5_IDX 5

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_6 register. */
#define ATON_DEBUG_TRACE_0_EVENT_6_IDX 6

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_7 register. */
#define ATON_DEBUG_TRACE_0_EVENT_7_IDX 7

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_8 register. */
#define ATON_DEBUG_TRACE_0_EVENT_8_IDX 8

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_9 register. */
#define ATON_DEBUG_TRACE_0_EVENT_9_IDX 9

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_10 register. */
#define ATON_DEBUG_TRACE_0_EVENT_10_IDX 10

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_11 register. */
#define ATON_DEBUG_TRACE_0_EVENT_11_IDX 11

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_12 register. */
#define ATON_DEBUG_TRACE_0_EVENT_12_IDX 12

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_13 register. */
#define ATON_DEBUG_TRACE_0_EVENT_13_IDX 13

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_14 register. */
#define ATON_DEBUG_TRACE_0_EVENT_14_IDX 14

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_15 register. */
#define ATON_DEBUG_TRACE_0_EVENT_15_IDX 15


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_0 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_0 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_1 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_1 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_10 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_10 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_10_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 10 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_11 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_11 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_11_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 11 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_12 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_12 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_12_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 12 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_13 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_13 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_13_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 13 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_14 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_14 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_14_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 14 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_15 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_15 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_15_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 15 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_2 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_2 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_3 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_3 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_3_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_4 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_4 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_4_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_5 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_5 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_5_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_6 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_6 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_6_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_7 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_7 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_7_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 7 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_8 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_8 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_8_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 8 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_9 register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_9 register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_EVENT_9_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 9 : -1)



/* *********************************************************************** EVENT_CNT register of one of the DEBUG_TRACE units *********************************************************************** */

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_0_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_0_CNT_IDX 0

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_1_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_1_CNT_IDX 1

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_2_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_2_CNT_IDX 2

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_3_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_3_CNT_IDX 3

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_4_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_4_CNT_IDX 4

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_5_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_5_CNT_IDX 5

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_6_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_6_CNT_IDX 6

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_7_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_7_CNT_IDX 7

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_8_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_8_CNT_IDX 8

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_9_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_9_CNT_IDX 9

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_10_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_10_CNT_IDX 10

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_11_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_11_CNT_IDX 11

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_12_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_12_CNT_IDX 12

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_13_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_13_CNT_IDX 13

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_14_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_14_CNT_IDX 14

/** Value of idx index (expansion index) for DEBUG_TRACE_0.EVENT_15_CNT register. */
#define ATON_DEBUG_TRACE_0_EVENT_15_CNT_IDX 15


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_0_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_0_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_0_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_10_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_10_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_10_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 10 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_11_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_11_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_11_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 11 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_12_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_12_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_12_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 12 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_13_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_13_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_13_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 13 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_14_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_14_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_14_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 14 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_15_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_15_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_15_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 15 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_1_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_1_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_1_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_2_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_2_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_2_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_3_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_3_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_3_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_4_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_4_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_4_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_5_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_5_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_5_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_6_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_6_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_6_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_7_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_7_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_7_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 7 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_8_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_8_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_8_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 8 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EVENT_9_CNT register given the index of one of the DEBUG_TRACE units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'DEBUG_TRACE_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EVENT_9_CNT register given the index \e UNIT of one of the DEBUG_TRACE units
 */

#define ATON_DEBUG_TRACE_EVENT_CNT_EVENT_9_CNT_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 9 : -1)



/*@}*/


/* ************************************************************************************************************************************************************************************************** */
/*                                                                                                                                                                                                    */
/*                                                                                          EPOCHCTRL units                                                                                           */
/*                                                                                                                                                                                                    */
/* ************************************************************************************************************************************************************************************************** */

/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */
/*                                                                                                                                                                                                    */
/*                                                                                          EPOCHCTRL units                                                                                           */
/*                                                                                                                                                                                                    */
/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */

/**
 * \name Macros of the EPOCHCTRL units
 */
/*@{*/

/*@}*/


/* ************************************************************************************************************************************************************************************************** */
/*                                                                                                                                                                                                    */
/*                                                                                           INTCTRL units                                                                                            */
/*                                                                                                                                                                                                    */
/* ************************************************************************************************************************************************************************************************** */

/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */
/*                                                                                                                                                                                                    */
/*                                                                                           INTCTRL units                                                                                            */
/*                                                                                                                                                                                                    */
/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */

/**
 * \name Macros of the INTCTRL units
 */
/*@{*/

/* ---------------------------------------------------------------------------- INT field of the INTCTRL.INTREG register ---------------------------------------------------------------------------- */

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRENG_0_0 register field. */
#define ATON_INTCTRL_0_INTREG_STRENG_0_0_IDX 0

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRENG_1_0 register field. */
#define ATON_INTCTRL_0_INTREG_STRENG_1_0_IDX 1

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRENG_2_0 register field. */
#define ATON_INTCTRL_0_INTREG_STRENG_2_0_IDX 2

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRENG_3_0 register field. */
#define ATON_INTCTRL_0_INTREG_STRENG_3_0_IDX 3

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRENG_4_0 register field. */
#define ATON_INTCTRL_0_INTREG_STRENG_4_0_IDX 4

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRENG_5_0 register field. */
#define ATON_INTCTRL_0_INTREG_STRENG_5_0_IDX 5

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRENG_0_1 register field. */
#define ATON_INTCTRL_0_INTREG_STRENG_0_1_IDX 6

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRENG_1_1 register field. */
#define ATON_INTCTRL_0_INTREG_STRENG_1_1_IDX 7

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRENG_2_1 register field. */
#define ATON_INTCTRL_0_INTREG_STRENG_2_1_IDX 8

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRENG_3_1 register field. */
#define ATON_INTCTRL_0_INTREG_STRENG_3_1_IDX 9

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRENG_4_1 register field. */
#define ATON_INTCTRL_0_INTREG_STRENG_4_1_IDX 10

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRENG_5_1 register field. */
#define ATON_INTCTRL_0_INTREG_STRENG_5_1_IDX 11

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.CONVACC_0 register field. */
#define ATON_INTCTRL_0_INTREG_CONVACC_0_IDX 12

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.EPOCHCTRL_0_0 register field. */
#define ATON_INTCTRL_0_INTREG_EPOCHCTRL_0_0_IDX 13

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.EPOCHCTRL_0_1 register field. */
#define ATON_INTCTRL_0_INTREG_EPOCHCTRL_0_1_IDX 14

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.EPOCHCTRL_0_2 register field. */
#define ATON_INTCTRL_0_INTREG_EPOCHCTRL_0_2_IDX 15

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.DEBUG_TRACE_0 register field. */
#define ATON_INTCTRL_0_INTREG_DEBUG_TRACE_0_IDX 16

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.BUSIF_0 register field. */
#define ATON_INTCTRL_0_INTREG_BUSIF_0_IDX 17

/** Value of idx index (expansion index) for INTCTRL_0.INTREG.STRSWITCH_0 register field. */
#define ATON_INTCTRL_0_INTREG_STRSWITCH_0_IDX 18


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_BUSIF_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 17 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the CONVACC_0 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the CONVACC_0 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_CONVACC_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 12 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DEBUG_TRACE_0 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DEBUG_TRACE_0 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_DEBUG_TRACE_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 16 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_0 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_0 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_EPOCHCTRL_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 13 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_1 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_1 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_EPOCHCTRL_0_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 14 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_2 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_2 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_EPOCHCTRL_0_2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 15 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_0_0 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_0_0 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRENG_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_0_1 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_0_1 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRENG_0_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_1_0 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_1_0 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRENG_1_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_1_1 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_1_1 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRENG_1_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 7 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_2_0 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_2_0 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRENG_2_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_2_1 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_2_1 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRENG_2_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 8 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_3_0 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_3_0 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRENG_3_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_3_1 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_3_1 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRENG_3_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 9 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_4_0 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_4_0 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRENG_4_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_4_1 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_4_1 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRENG_4_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 10 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_5_0 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_5_0 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRENG_5_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_5_1 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_5_1 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRENG_5_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 11 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the INTREG register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the INTREG register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTREG_INT_STRSWITCH_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 18 : -1)



/* ---------------------------------------------------------------------------- INT field of the INTCTRL.INTSET register ---------------------------------------------------------------------------- */

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRENG_0_0 register field. */
#define ATON_INTCTRL_0_INTSET_STRENG_0_0_IDX 0

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRENG_1_0 register field. */
#define ATON_INTCTRL_0_INTSET_STRENG_1_0_IDX 1

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRENG_2_0 register field. */
#define ATON_INTCTRL_0_INTSET_STRENG_2_0_IDX 2

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRENG_3_0 register field. */
#define ATON_INTCTRL_0_INTSET_STRENG_3_0_IDX 3

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRENG_4_0 register field. */
#define ATON_INTCTRL_0_INTSET_STRENG_4_0_IDX 4

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRENG_5_0 register field. */
#define ATON_INTCTRL_0_INTSET_STRENG_5_0_IDX 5

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRENG_0_1 register field. */
#define ATON_INTCTRL_0_INTSET_STRENG_0_1_IDX 6

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRENG_1_1 register field. */
#define ATON_INTCTRL_0_INTSET_STRENG_1_1_IDX 7

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRENG_2_1 register field. */
#define ATON_INTCTRL_0_INTSET_STRENG_2_1_IDX 8

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRENG_3_1 register field. */
#define ATON_INTCTRL_0_INTSET_STRENG_3_1_IDX 9

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRENG_4_1 register field. */
#define ATON_INTCTRL_0_INTSET_STRENG_4_1_IDX 10

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRENG_5_1 register field. */
#define ATON_INTCTRL_0_INTSET_STRENG_5_1_IDX 11

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.CONVACC_0 register field. */
#define ATON_INTCTRL_0_INTSET_CONVACC_0_IDX 12

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.EPOCHCTRL_0_0 register field. */
#define ATON_INTCTRL_0_INTSET_EPOCHCTRL_0_0_IDX 13

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.EPOCHCTRL_0_1 register field. */
#define ATON_INTCTRL_0_INTSET_EPOCHCTRL_0_1_IDX 14

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.EPOCHCTRL_0_2 register field. */
#define ATON_INTCTRL_0_INTSET_EPOCHCTRL_0_2_IDX 15

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.DEBUG_TRACE_0 register field. */
#define ATON_INTCTRL_0_INTSET_DEBUG_TRACE_0_IDX 16

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.BUSIF_0 register field. */
#define ATON_INTCTRL_0_INTSET_BUSIF_0_IDX 17

/** Value of idx index (expansion index) for INTCTRL_0.INTSET.STRSWITCH_0 register field. */
#define ATON_INTCTRL_0_INTSET_STRSWITCH_0_IDX 18


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_BUSIF_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 17 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the CONVACC_0 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the CONVACC_0 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_CONVACC_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 12 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DEBUG_TRACE_0 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DEBUG_TRACE_0 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_DEBUG_TRACE_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 16 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_0 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_0 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_EPOCHCTRL_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 13 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_1 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_1 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_EPOCHCTRL_0_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 14 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_2 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_2 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_EPOCHCTRL_0_2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 15 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_0_0 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_0_0 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRENG_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_0_1 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_0_1 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRENG_0_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_1_0 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_1_0 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRENG_1_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_1_1 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_1_1 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRENG_1_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 7 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_2_0 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_2_0 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRENG_2_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_2_1 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_2_1 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRENG_2_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 8 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_3_0 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_3_0 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRENG_3_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_3_1 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_3_1 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRENG_3_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 9 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_4_0 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_4_0 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRENG_4_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_4_1 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_4_1 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRENG_4_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 10 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_5_0 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_5_0 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRENG_5_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_5_1 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_5_1 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRENG_5_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 11 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the INTSET register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the INTSET register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTSET_INT_STRSWITCH_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 18 : -1)



/* ---------------------------------------------------------------------------- INT field of the INTCTRL.INTCLR register ---------------------------------------------------------------------------- */

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRENG_0_0 register field. */
#define ATON_INTCTRL_0_INTCLR_STRENG_0_0_IDX 0

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRENG_1_0 register field. */
#define ATON_INTCTRL_0_INTCLR_STRENG_1_0_IDX 1

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRENG_2_0 register field. */
#define ATON_INTCTRL_0_INTCLR_STRENG_2_0_IDX 2

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRENG_3_0 register field. */
#define ATON_INTCTRL_0_INTCLR_STRENG_3_0_IDX 3

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRENG_4_0 register field. */
#define ATON_INTCTRL_0_INTCLR_STRENG_4_0_IDX 4

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRENG_5_0 register field. */
#define ATON_INTCTRL_0_INTCLR_STRENG_5_0_IDX 5

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRENG_0_1 register field. */
#define ATON_INTCTRL_0_INTCLR_STRENG_0_1_IDX 6

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRENG_1_1 register field. */
#define ATON_INTCTRL_0_INTCLR_STRENG_1_1_IDX 7

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRENG_2_1 register field. */
#define ATON_INTCTRL_0_INTCLR_STRENG_2_1_IDX 8

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRENG_3_1 register field. */
#define ATON_INTCTRL_0_INTCLR_STRENG_3_1_IDX 9

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRENG_4_1 register field. */
#define ATON_INTCTRL_0_INTCLR_STRENG_4_1_IDX 10

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRENG_5_1 register field. */
#define ATON_INTCTRL_0_INTCLR_STRENG_5_1_IDX 11

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.CONVACC_0 register field. */
#define ATON_INTCTRL_0_INTCLR_CONVACC_0_IDX 12

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.EPOCHCTRL_0_0 register field. */
#define ATON_INTCTRL_0_INTCLR_EPOCHCTRL_0_0_IDX 13

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.EPOCHCTRL_0_1 register field. */
#define ATON_INTCTRL_0_INTCLR_EPOCHCTRL_0_1_IDX 14

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.EPOCHCTRL_0_2 register field. */
#define ATON_INTCTRL_0_INTCLR_EPOCHCTRL_0_2_IDX 15

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.DEBUG_TRACE_0 register field. */
#define ATON_INTCTRL_0_INTCLR_DEBUG_TRACE_0_IDX 16

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.BUSIF_0 register field. */
#define ATON_INTCTRL_0_INTCLR_BUSIF_0_IDX 17

/** Value of idx index (expansion index) for INTCTRL_0.INTCLR.STRSWITCH_0 register field. */
#define ATON_INTCTRL_0_INTCLR_STRSWITCH_0_IDX 18


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_BUSIF_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 17 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the CONVACC_0 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the CONVACC_0 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_CONVACC_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 12 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DEBUG_TRACE_0 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DEBUG_TRACE_0 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_DEBUG_TRACE_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 16 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_0 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_0 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_EPOCHCTRL_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 13 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_1 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_1 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_EPOCHCTRL_0_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 14 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_2 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_2 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_EPOCHCTRL_0_2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 15 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_0_0 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_0_0 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRENG_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_0_1 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_0_1 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRENG_0_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_1_0 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_1_0 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRENG_1_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_1_1 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_1_1 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRENG_1_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 7 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_2_0 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_2_0 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRENG_2_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_2_1 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_2_1 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRENG_2_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 8 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_3_0 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_3_0 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRENG_3_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_3_1 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_3_1 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRENG_3_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 9 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_4_0 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_4_0 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRENG_4_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_4_1 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_4_1 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRENG_4_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 10 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_5_0 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_5_0 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRENG_5_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_5_1 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_5_1 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRENG_5_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 11 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the INTCLR register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the INTCLR register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTCLR_INT_STRSWITCH_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 18 : -1)



/* ************************************************************************* INTORMSK register of one of the INTCTRL units ************************************************************************** */

/** Value of iom index (expansion index) for INTCTRL_0.INTORMSK0 register. */
#define ATON_INTCTRL_0_INTORMSK0_IOM 0

/** Value of iom index (expansion index) for INTCTRL_0.INTORMSK1 register. */
#define ATON_INTCTRL_0_INTORMSK1_IOM 1

/** Value of iom index (expansion index) for INTCTRL_0.INTORMSK2 register. */
#define ATON_INTCTRL_0_INTORMSK2_IOM 2

/** Value of iom index (expansion index) for INTCTRL_0.INTORMSK3 register. */
#define ATON_INTCTRL_0_INTORMSK3_IOM 3


/**
 * Return the value of iom index (expansion index) necessary for obtaining the INTORMSK0 register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of iom index (expansion index) necessary for obtaining the INTORMSK0 register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INTORMSK0_GET_IOM(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of iom index (expansion index) necessary for obtaining the INTORMSK1 register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of iom index (expansion index) necessary for obtaining the INTORMSK1 register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INTORMSK1_GET_IOM(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of iom index (expansion index) necessary for obtaining the INTORMSK2 register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of iom index (expansion index) necessary for obtaining the INTORMSK2 register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INTORMSK2_GET_IOM(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of iom index (expansion index) necessary for obtaining the INTORMSK3 register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of iom index (expansion index) necessary for obtaining the INTORMSK3 register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INTORMSK3_GET_IOM(UNIT) \
  (((UNIT) < 1) ? 3 : -1)



/* --------------------------------------------------------------------------- INT field of the INTCTRL.INTORMSK register --------------------------------------------------------------------------- */

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRENG_0_0 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRENG_0_0_IDX 0

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRENG_1_0 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRENG_1_0_IDX 1

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRENG_2_0 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRENG_2_0_IDX 2

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRENG_3_0 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRENG_3_0_IDX 3

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRENG_4_0 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRENG_4_0_IDX 4

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRENG_5_0 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRENG_5_0_IDX 5

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRENG_0_1 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRENG_0_1_IDX 6

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRENG_1_1 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRENG_1_1_IDX 7

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRENG_2_1 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRENG_2_1_IDX 8

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRENG_3_1 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRENG_3_1_IDX 9

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRENG_4_1 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRENG_4_1_IDX 10

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRENG_5_1 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRENG_5_1_IDX 11

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.CONVACC_0 register field. */
#define ATON_INTCTRL_0_INTORMSK_CONVACC_0_IDX 12

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.EPOCHCTRL_0_0 register field. */
#define ATON_INTCTRL_0_INTORMSK_EPOCHCTRL_0_0_IDX 13

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.EPOCHCTRL_0_1 register field. */
#define ATON_INTCTRL_0_INTORMSK_EPOCHCTRL_0_1_IDX 14

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.EPOCHCTRL_0_2 register field. */
#define ATON_INTCTRL_0_INTORMSK_EPOCHCTRL_0_2_IDX 15

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.DEBUG_TRACE_0 register field. */
#define ATON_INTCTRL_0_INTORMSK_DEBUG_TRACE_0_IDX 16

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.BUSIF_0 register field. */
#define ATON_INTCTRL_0_INTORMSK_BUSIF_0_IDX 17

/** Value of idx index (expansion index) for INTCTRL_0.INTORMSK.STRSWITCH_0 register field. */
#define ATON_INTCTRL_0_INTORMSK_STRSWITCH_0_IDX 18


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_BUSIF_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 17 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the CONVACC_0 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the CONVACC_0 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_CONVACC_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 12 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DEBUG_TRACE_0 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DEBUG_TRACE_0 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_DEBUG_TRACE_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 16 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_0 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_0 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_EPOCHCTRL_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 13 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_1 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_1 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_EPOCHCTRL_0_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 14 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_2 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_2 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_EPOCHCTRL_0_2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 15 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_0_0 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_0_0 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRENG_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_0_1 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_0_1 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRENG_0_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_1_0 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_1_0 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRENG_1_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_1_1 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_1_1 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRENG_1_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 7 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_2_0 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_2_0 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRENG_2_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_2_1 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_2_1 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRENG_2_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 8 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_3_0 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_3_0 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRENG_3_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_3_1 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_3_1 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRENG_3_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 9 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_4_0 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_4_0 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRENG_4_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_4_1 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_4_1 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRENG_4_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 10 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_5_0 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_5_0 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRENG_5_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_5_1 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_5_1 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRENG_5_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 11 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the INTORMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the INTORMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTORMSK_INT_STRSWITCH_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 18 : -1)



/* ************************************************************************* INTANDMSK register of one of the INTCTRL units ************************************************************************* */

/** Value of iam index (expansion index) for INTCTRL_0.INTANDMSK0 register. */
#define ATON_INTCTRL_0_INTANDMSK0_IAM 0

/** Value of iam index (expansion index) for INTCTRL_0.INTANDMSK1 register. */
#define ATON_INTCTRL_0_INTANDMSK1_IAM 1

/** Value of iam index (expansion index) for INTCTRL_0.INTANDMSK2 register. */
#define ATON_INTCTRL_0_INTANDMSK2_IAM 2

/** Value of iam index (expansion index) for INTCTRL_0.INTANDMSK3 register. */
#define ATON_INTCTRL_0_INTANDMSK3_IAM 3


/**
 * Return the value of iam index (expansion index) necessary for obtaining the INTANDMSK0 register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of iam index (expansion index) necessary for obtaining the INTANDMSK0 register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INTANDMSK0_GET_IAM(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of iam index (expansion index) necessary for obtaining the INTANDMSK1 register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of iam index (expansion index) necessary for obtaining the INTANDMSK1 register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INTANDMSK1_GET_IAM(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of iam index (expansion index) necessary for obtaining the INTANDMSK2 register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of iam index (expansion index) necessary for obtaining the INTANDMSK2 register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INTANDMSK2_GET_IAM(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of iam index (expansion index) necessary for obtaining the INTANDMSK3 register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of iam index (expansion index) necessary for obtaining the INTANDMSK3 register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INTANDMSK3_GET_IAM(UNIT) \
  (((UNIT) < 1) ? 3 : -1)



/* -------------------------------------------------------------------------- INT field of the INTCTRL.INTANDMSK register --------------------------------------------------------------------------- */

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRENG_0_0 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRENG_0_0_IDX 0

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRENG_1_0 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRENG_1_0_IDX 1

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRENG_2_0 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRENG_2_0_IDX 2

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRENG_3_0 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRENG_3_0_IDX 3

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRENG_4_0 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRENG_4_0_IDX 4

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRENG_5_0 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRENG_5_0_IDX 5

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRENG_0_1 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRENG_0_1_IDX 6

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRENG_1_1 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRENG_1_1_IDX 7

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRENG_2_1 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRENG_2_1_IDX 8

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRENG_3_1 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRENG_3_1_IDX 9

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRENG_4_1 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRENG_4_1_IDX 10

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRENG_5_1 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRENG_5_1_IDX 11

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.CONVACC_0 register field. */
#define ATON_INTCTRL_0_INTANDMSK_CONVACC_0_IDX 12

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.EPOCHCTRL_0_0 register field. */
#define ATON_INTCTRL_0_INTANDMSK_EPOCHCTRL_0_0_IDX 13

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.EPOCHCTRL_0_1 register field. */
#define ATON_INTCTRL_0_INTANDMSK_EPOCHCTRL_0_1_IDX 14

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.EPOCHCTRL_0_2 register field. */
#define ATON_INTCTRL_0_INTANDMSK_EPOCHCTRL_0_2_IDX 15

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.DEBUG_TRACE_0 register field. */
#define ATON_INTCTRL_0_INTANDMSK_DEBUG_TRACE_0_IDX 16

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.BUSIF_0 register field. */
#define ATON_INTCTRL_0_INTANDMSK_BUSIF_0_IDX 17

/** Value of idx index (expansion index) for INTCTRL_0.INTANDMSK.STRSWITCH_0 register field. */
#define ATON_INTCTRL_0_INTANDMSK_STRSWITCH_0_IDX 18


/**
 * Return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the BUSIF_0 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_BUSIF_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 17 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the CONVACC_0 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the CONVACC_0 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_CONVACC_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 12 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DEBUG_TRACE_0 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DEBUG_TRACE_0 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_DEBUG_TRACE_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 16 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_0 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_0 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_EPOCHCTRL_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 13 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_1 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_1 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_EPOCHCTRL_0_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 14 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_2 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the EPOCHCTRL_0_2 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_EPOCHCTRL_0_2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 15 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_0_0 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_0_0 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRENG_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_0_1 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_0_1 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRENG_0_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_1_0 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_1_0 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRENG_1_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_1_1 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_1_1 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRENG_1_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 7 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_2_0 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_2_0 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRENG_2_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_2_1 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_2_1 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRENG_2_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 8 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_3_0 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_3_0 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRENG_3_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_3_1 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_3_1 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRENG_3_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 9 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_4_0 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_4_0 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRENG_4_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_4_1 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_4_1 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRENG_4_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 10 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_5_0 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_5_0 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRENG_5_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRENG_5_1 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRENG_5_1 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRENG_5_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 11 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the INTANDMSK register given the index of one of the INTCTRL units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'INTCTRL_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the STRSWITCH_0 field of the INTANDMSK register given the index \e UNIT of one of the INTCTRL units
 */

#define ATON_INTCTRL_INTANDMSK_INT_STRSWITCH_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 18 : -1)



/*@}*/


/* ************************************************************************************************************************************************************************************************** */
/*                                                                                                                                                                                                    */
/*                                                                                             POOL units                                                                                             */
/*                                                                                                                                                                                                    */
/* ************************************************************************************************************************************************************************************************** */

/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */
/*                                                                                                                                                                                                    */
/*                                                                                             POOL units                                                                                             */
/*                                                                                                                                                                                                    */
/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */

/**
 * \name Macros of the POOL units
 */
/*@{*/

/*@}*/


/* ************************************************************************************************************************************************************************************************** */
/*                                                                                                                                                                                                    */
/*                                                                                            STRENG units                                                                                            */
/*                                                                                                                                                                                                    */
/* ************************************************************************************************************************************************************************************************** */

/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */
/*                                                                                                                                                                                                    */
/*                                                                                            STRENG units                                                                                            */
/*                                                                                                                                                                                                    */
/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */

/**
 * \name Macros of the STRENG units
 */
/*@{*/

/*@}*/


/* ************************************************************************************************************************************************************************************************** */
/*                                                                                                                                                                                                    */
/*                                                                                          STRSWITCH units                                                                                           */
/*                                                                                                                                                                                                    */
/* ************************************************************************************************************************************************************************************************** */

/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */
/*                                                                                                                                                                                                    */
/*                                                                                          STRSWITCH units                                                                                           */
/*                                                                                                                                                                                                    */
/* -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- */

/**
 * \name Macros of the STRSWITCH units
 */
/*@{*/

/* *************************************************************************** DST register of one of the STRSWITCH units *************************************************************************** */

/** Value of idx index (expansion index) for STRSWITCH_0.DSTSTRENG_0_0 register. */
#define ATON_STRSWITCH_0_DSTSTRENG_0_0_IDX 0

/** Value of idx index (expansion index) for STRSWITCH_0.DSTSTRENG_1_0 register. */
#define ATON_STRSWITCH_0_DSTSTRENG_1_0_IDX 1

/** Value of idx index (expansion index) for STRSWITCH_0.DSTSTRENG_2_0 register. */
#define ATON_STRSWITCH_0_DSTSTRENG_2_0_IDX 2

/** Value of idx index (expansion index) for STRSWITCH_0.DSTSTRENG_3_0 register. */
#define ATON_STRSWITCH_0_DSTSTRENG_3_0_IDX 3

/** Value of idx index (expansion index) for STRSWITCH_0.DSTSTRENG_4_0 register. */
#define ATON_STRSWITCH_0_DSTSTRENG_4_0_IDX 4

/** Value of idx index (expansion index) for STRSWITCH_0.DSTSTRENG_5_0 register. */
#define ATON_STRSWITCH_0_DSTSTRENG_5_0_IDX 5

/** Value of idx index (expansion index) for STRSWITCH_0.DSTCONVACC_0_0 register. */
#define ATON_STRSWITCH_0_DSTCONVACC_0_0_IDX 6

/** Value of idx index (expansion index) for STRSWITCH_0.DSTCONVACC_0_1 register. */
#define ATON_STRSWITCH_0_DSTCONVACC_0_1_IDX 7

/** Value of idx index (expansion index) for STRSWITCH_0.DSTCONVACC_0_2 register. */
#define ATON_STRSWITCH_0_DSTCONVACC_0_2_IDX 8

/** Value of idx index (expansion index) for STRSWITCH_0.DSTACTIV_0_0 register. */
#define ATON_STRSWITCH_0_DSTACTIV_0_0_IDX 9

/** Value of idx index (expansion index) for STRSWITCH_0.DSTARITH_0_0 register. */
#define ATON_STRSWITCH_0_DSTARITH_0_0_IDX 10

/** Value of idx index (expansion index) for STRSWITCH_0.DSTARITH_0_1 register. */
#define ATON_STRSWITCH_0_DSTARITH_0_1_IDX 11

/** Value of idx index (expansion index) for STRSWITCH_0.DSTARITH_1_0 register. */
#define ATON_STRSWITCH_0_DSTARITH_1_0_IDX 12

/** Value of idx index (expansion index) for STRSWITCH_0.DSTARITH_1_1 register. */
#define ATON_STRSWITCH_0_DSTARITH_1_1_IDX 13

/** Value of idx index (expansion index) for STRSWITCH_0.DSTPOOL_0_0 register. */
#define ATON_STRSWITCH_0_DSTPOOL_0_0_IDX 14


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTACTIV_0_0 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTACTIV_0_0 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTACTIV_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 9 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTARITH_0_0 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTARITH_0_0 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTARITH_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 10 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTARITH_0_1 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTARITH_0_1 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTARITH_0_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 11 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTARITH_1_0 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTARITH_1_0 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTARITH_1_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 12 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTARITH_1_1 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTARITH_1_1 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTARITH_1_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 13 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTCONVACC_0_0 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTCONVACC_0_0 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTCONVACC_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 6 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTCONVACC_0_1 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTCONVACC_0_1 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTCONVACC_0_1_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 7 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTCONVACC_0_2 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTCONVACC_0_2 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTCONVACC_0_2_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 8 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTPOOL_0_0 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTPOOL_0_0 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTPOOL_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 14 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTSTRENG_0_0 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTSTRENG_0_0 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTSTRENG_0_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 0 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTSTRENG_1_0 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTSTRENG_1_0 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTSTRENG_1_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 1 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTSTRENG_2_0 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTSTRENG_2_0 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTSTRENG_2_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 2 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTSTRENG_3_0 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTSTRENG_3_0 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTSTRENG_3_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 3 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTSTRENG_4_0 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTSTRENG_4_0 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTSTRENG_4_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 4 : -1)


/**
 * Return the value of idx index (expansion index) necessary for obtaining the DSTSTRENG_5_0 register given the index of one of the STRSWITCH units.
 *
 * \param[in] UNIT is the index of the unit template instance that must be considered:
 *                 - UNIT == 0: 'STRSWITCH_0'
 *
 * \return the value of idx index (expansion index) necessary for obtaining the DSTSTRENG_5_0 register given the index \e UNIT of one of the STRSWITCH units
 */

#define ATON_STRSWITCH_DST_DSTSTRENG_5_0_GET_IDX(UNIT) \
  (((UNIT) < 1) ? 5 : -1)



/*@}*/


#ifdef __cplusplus
}
#endif

#endif    // #ifndef ATON_IDXS_H
